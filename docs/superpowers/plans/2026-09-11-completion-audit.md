# 구현 완료 감사: 남은 증거와 작업

이 문서는 PRD 14.2의 완료 조건을 대체하지 않는다. 테스트 개수나 개별 수정만으로 전체 완료를 선언하지 않는다.

## 현재 외부 선행 조건 재확인

- 구현 검증 기준 커밋은 `2543de3b49f704e7aaecbf91fbe56c2cf2798564`다. [초안 PR #2](https://github.com/nicekon/zendesk-mcp-server-kon/pull/2)에 게시됐고 [CI 실행 34670609455](https://github.com/nicekon/zendesk-mcp-server-kon/actions/runs/34670609455)의 5개 작업이 성공했다. 이후 변경의 검증 여부는 PR의 최신 head와 check를 별도로 확인한다.
- 사용자 요청으로 다시 시작한 브라우저 로그인 이후 `uv run zendesk check --probe`가 exit 0/ok=true/auth_mode=oauth로 성공했다. 기본 저장 구성의 `reauthorization_required`는 해소됐다. read_only와 빈 active_write_gates를 확인했다.
- support-only 실제 검색의 `required_scopes: [read]`에 대해 사용자가 정책 예외를 승인했다. Support 활성 시만 broad read를 요청하도록 코드·PRD·README·migration을 변경했다. 이 권한은 검색 전용이 아닌 역할 범위 내 모든 GET 접근이며 broad write와 서버 쓰기 gate는 변경하지 않는다. 재로그인 후 실제 ticket search와 Search Export 한 페이지 조회가 각각 1건/ok=true로 성공했다. 아래 날짜별 broad read 금지/정책 결정 대기 기록은 승인 이전의 이력이다.
- 원격 Linux Python 3.10–3.12에서 test→build→wheel install→MCP handshake가 통과했다. macOS/Windows에서는 패키지 설치·MCP handshake가 통과했다. Windows OAuth는 private storage 권한을 보장하지 못할 때 unsupported로 중단하며, 패키지 smoke 성공을 OAuth 지원으로 해석하지 않는다.
- 사용자는 별도 테스트 계정이 없음을 확인했고 재요청하지 않기로 했다. 실제 쓰기·사람 승인·외부 업로드 E2E는 출시 검증의 미검증 항목으로 남긴다. 이것을 코드 검토·자동 테스트·PR 유지의 선행 조건으로 반복 제시하지 않는다. 운영 계정 쓰기로 대체하지 않으며 PRD 14.2 전체 달성을 주장하지 않는다. 병합·배포도 수행하지 않았다.

## 최근 코드 검증

- PRD 12.1 쓰기 성공 상태: 공통 HTTP JSON/binary 성공 응답에 top-level `operation_state=applied`를 추가하고, 응답을 재구성하는 subscription·badge icon·publish에도 상태와 write request ID를 보존했다. GET/HEAD에는 붙이지 않는다. HTTP 4개 쓰기 method·binary upload 및 3개 wrapper에서 누락을 실패 재현했다. 실제 MCP→공통 client→MockTransport 전송 경계도 검증했다. publish는 재조회가 실패하거나 지정 locale/draft=false가 아니면 non-retryable `outcome_unknown`/unknown으로 반환하고 write request ID를 보존한다. 5개 실패·불일치 회귀와 브랜드별 성공을 검증했다. 전체 536 passed; 실제 외부 쓰기 성공을 뜻하지 않는다. 공통 output schema의 해당 선택 필드만 frozen fixture에 추가했다.
- 전체 MCP 계약 회귀: `tests/fixtures/tool-contracts.json`에 101개 도구의 inputSchema/outputSchema/annotations 기준을 고정했다. `test_mcp_catalog_matches_frozen_input_output_and_risk_contracts`는 실제 MCP 목록 응답과 각 항목을 정확히 비교한다. public reply의 approval_token 삭제, 출력 required 삭제, readOnlyHint 반전의 세 런타임 변형을 각각 실패로 탐지했고 정상 전체 523 passed. 기준은 현 구현의 계약 회귀 방지용이며 모든 schema의 제품 의미가 PRD와 일치한다는 독립 증거는 아니다. 의도된 계약 변경은 해당 fixture 항목과 PRD·migration을 함께 검토하고 전체 기준을 자동 재생성하지 않는다.
- Canonical 이름 대조: PRD 8장의 99개 중 6개가 구현에서 다른 이름을 사용했다. 문서 기준 이름으로 등록·dispatch·승인 identity를 교정하고 Guide capability와 badge 해제의 destructive annotation을 유지했다. 기존 CSAT selector와 badge icon uploader도 문서 목록에 명시해 101개가 누락/별칭 없이 일치한다. 기존 exact name 테스트에서 실패 재현 후 통과했으며 실제 MCP factory의 6개 호출·capability별 노출·승인 identity·read-only 차단 테스트를 추가했다. 로컬 전체 522 passed. 이는 전체 도구 schema의 exact snapshot 또는 기능 합집합 전체 감사 완료를 의미하지 않는다. 변경 전 이름으로 발급한 승인은 새 preview가 필요하다.
- 티켓 생성의 최초 comment에 `public=false`를 명시했다. `test_create_ticket_uses_a_validated_standard_write_payload`와 `test_create_ticket_accepts_assignment_and_custom_fields`에서 내부 메모 전송 payload를 검증한다.
- 태그 추가·삭제는 공통 `_change_ticket_tag`에서 조회한 updated_at에 safe_update를 결합한다. `test_ticket_tag_shortcuts_preserve_concurrent_changes`, `test_ticket_tag_changes_reject_invalid_timestamps_without_writing`, `test_ticket_tag_noops_and_read_only_never_write`가 충돌·잘못된 시각·no-op·쓰기 gate를 검증한다.
- 직접 closed 변경과 상태 shortcut은 공통 승인 경로를 사용한다. `test_ticket_closure_requires_preview_destructive_gate_and_payload_approval`와 `test_ticket_closure_mcp_dispatch_preserves_approval_options`가 preview의 write 0회, gate, payload 결합, single-use 및 MCP 전달을 검증한다. 다른 일반 상태 변경은 기존 표준 쓰기 동작을 유지한다.
- 위 변경 후 로컬 전체 521 passed와 원격 CI 성공을 확인했다. 테스트는 실제 Zendesk 쓰기가 아닌 통제된 응답과 전송 경계 검증이며, 실환경 성공 증거로 대체하지 않는다.

## 누적 점검 이력

아래의 현재/미완료/대기 표현과 테스트 개수는 각 점검 당시의 기록이다. 최신 상태는 위의 검증 기준과 PR의 해당 커밋 증거를 우선한다.

- PR 사전 리뷰: limit=99의 검색 cursor=990에서 upstream 99건 페이지가 1,000건 검색 한계를 넘는 오류를 재현했다. logical page/next_page 의미는 유지하고 1,000의 약수가 아닌 upstream page size는 100으로 정규화했다. 공통 offset 수집기도 max_results 잔여량까지만 선택한다. 99건씩 전체 재개와 legacy page=2, 별도 max_results=950 경계를 실패 재현 후 검증했다. 테스트 계정이 없다는 사용자 확인에 따라 실제 쓰기 E2E는 보류한다. PR 게시 허용은 병합·배포 또는 운영 쓰기 허용이 아니다.
- 재로그인 후 live 읽기 재검증: 저장 OAuth를 사용하는 실제 factory에서 ticket search와 export page adapter를 각각 limit=1로 호출해 성공했다. Operations의 groups/brands/ticket fields/forms/custom statuses/views/macros/triggers도 각각 limit=1, 1건/ok=true였다. 출력은 작업명·성공 여부·건수·오류 코드로 제한했고 고객 본문/ID/token/cursor는 기록하지 않았다. 이는 해당 목록의 소량 조회 증거이며 전체 pagination, 대량 export 파일 생성, 쓰기 또는 원격 CI 증거는 아니다. 아래 timeout/reauthorization_required/live fields permission_denied 기록은 재로그인 전의 이력이다.
- 승인 예외 패키지/버전 검증: `/tmp/zendesk-read-scope-package-sGs1Qq`에서 새 sdist→wheel과 독립 Python 3.12 설치를 확인했다. 저장소 밖 import, Support에만 read 포함, read_only 유지, 실제 설치 stdio 101개 도구를 검증했다. 과거 임시 3.10/3.11 환경은 사라져 새 격리 환경을 만들었으며 각각 전체 514 passed. 재로그인 세션은 브라우저 callback 없이 180초 후 OAuth login timed out/exit 1로 종료됐다. 진행 중 로그인으로 취급하지 않으며 사용자 준비 전 재실행하지 않는다. live 검색·원격 CI·sandbox writes는 미검증이다.
- 승인된 Support broad-read 예외 검증: 실제 authorization URL에 Support 활성 시만 read가 포함되고, 다른 모든 capability만 활성화하면 포함되지 않음을 검증했다. 기본 쓰기 scope 없음/read_only 유지/log_time 차단, 기존 read 없는 Support grant의 재로그인 요구와 파일 불변을 확인했다. 전체 514 passed, diff check 통과. 현재 `zendesk check`는 예상대로 reauthorization_required이며 새 grant의 브라우저 동의와 live 검색 검증은 아직 수행하지 않았다.
- **Time Tracking 앱:** 기존 audit-only 기능 누락을 보완해 계정별 두 필드 설정/조회/누적 갱신 및 safe_update 충돌 방지를 구현했다. MCP dispatch 설정 전달, 내부 메모와 단일 PUT, malformed/null/missing/duplicate 필드, read_only·conflict 및 큰 입력 경계를 테스트했고 전체 477 passed. 기존 audit backend도 유지한다. 실제 앱 필드 읽기/승인된 sandbox 쓰기/최신 설치 검증은 남으며 자세한 근거는 `2026-09-12-time-tracking-app-gap.md`에 기록했다.
- **Support 대화:** 첫 페이지만 반환하던 문제는 수정했다. limit/cursor와 완전성 표시, 첨부/문맥 소비자의 전체 페이지 수집, channel 보존 및 현재 작성자 역할에 따른 side 보강을 구현하고 회귀/MCP/live 제한 조회를 확인했다. PRD 7.2의 7개 named channel과 unknown 문자열/숫자 channel 보존은 `test_conversation_normalizes_channel_without_losing_original_fields`에서 검증한다. 과거 작성 시점 역할 복원과 transcript 내부 재해석은 PRD에 별도 요구가 없어 필수 누락으로 취급하지 않는다. 실환경 모든 채널 데이터로 검증한 것은 아니며 archived-ticket audit 동작은 미검증이다.
- Operations 목록 재점검: groups/group users/brands/ticket fields/forms/custom statuses/views/view tickets/macros/triggers와 사용자 검색은 현재 MCP limit/cursor 입력 및 표준 목록 응답에 연결되어 있다. cursor 또는 공식 offset/전체 목록 방식으로 구현했고, 11개 도구의 실제 dispatch 통합 테스트가 있다. 이전의 “모두 첫 페이지만 반환” 판단은 폐기한다. live ticket fields는 permission_denied이므로 전체 endpoint의 실제 접근 성공으로 해석하지 않는다.
- PRD 13.1 공용 rate limiter: 현재 client.py의 _COOLDOWNS/_COOLDOWN_LOCK이 tenant별 429 cooldown을 공유한다. request와 인증된 download 경로에 연결되어 있으며 서로 다른 client 인스턴스, 더 긴 병렬 cooldown 보존, tenant 격리, 대기 예산 테스트가 있다. 이전의 “공유 limiter 없음” 판단은 폐기한다. 전체 rate-limit 정책 및 실환경 동시 호출 검증의 완료를 뜻하지는 않는다.
- Community 사용자 이미지 upload: 현재 64 KiB 청크를 TemporaryFile에 기록하며 크기와 SHA-256을 계산하고, 승인에 일치하는 사본을 iterator로 전송한다. 파일 경로는 dir_fd/no-follow로 열며 미지원 플랫폼은 unsupported다. 이전의 read(2_000_001) 메모리 적재 판단은 폐기한다. 실제 presigned 서버의 전송 호환성, Windows 지원 및 승인된 sandbox upload E2E는 남아 있다.
- OAuth 검색 권한 충돌(2026-09-11 live): 저장된 OAuth의 `tickets:read` 등 리소스별 scope로 `/api/v2/tickets.json`은 HTTP 200이지만 `/api/v2/search.json`과 `/api/v2/search/export.json`은 HTTP 403이다. 일반 검색 응답은 `You are missing the following required scopes: read`를 명시했다. `type:ticket` 조건으로도 동일하다. 따라서 PRD의 broad `read` 금지와 OAuth 검색 전체 지원은 현재 API에서 동시에 입증되지 않는다. scope는 변경하지 않았으며 검색 성공으로 처리하거나 목록 스캔으로 대체하지 않는다. 공통 요청 클라이언트는 확인된 응답을 `permission_denied`와 `required_scopes: [read]`로 전달하되 임의 upstream 본문은 노출하지 않는다. [공식 scope 설명](https://developer.zendesk.com/api-reference/ticketing/oauth/grant_type_tokens/#scope)은 broad read가 모든 GET 리소스를 허용함을 명시한다. 해결에는 검색용 broad read의 명시적 정책 예외 또는 Zendesk가 지원하는 제한 scope 대안의 추가 확인이 필요하다.
- Windows OAuth: ACL이 구현되지 않은 플랫폼에서는 PRD 13.2가 허용하는 대안대로 OAuth 구성·기존 토큰 읽기·토큰 저장을 `unsupported`로 중단한다. 이전 POSIX 검사 생략 동작을 제거했다. 이는 보안 fail-closed 대안이며 Windows OAuth 사용 가능을 뜻하지 않는다. ACL 구현 자체는 이 PRD의 필수 미완료 작업으로 추가하지 않는다. Windows의 실제 wheel 설치·stdio 실행 검증은 여전히 필요하다.
- Windows 실행: cache 사용자 식별은 _cache_user_id의 SID 경로로 처리한다. AuditLog.record의 fchmod 부재 응답 오류도 수정했으며 권한 함수가 없으면 파일 생성 없이 unsupported 경고를 남기고 도구 결과를 보존한다. 실제 Windows 실행과 사용자 전용 ACL 기반 저장은 여전히 미검증/미구현이다. PRD 13.2는 token/config 권한을 보장하지 못하면 명확한 unsupported 중단을 허용하므로, 이 보안 경계의 구현과 Windows OAuth 사용 가능 여부는 별도 판단한다.
- Macro: 현재는 읽기 preview 뒤 ticket PUT 한 번이므로 PRD 7.3의 다중 쓰기 부분 성공 경로는 없다. 적용 여부가 unknown/partial인 오류에는 ticket_id, macro_id, 수동 audit 확인 안내를 추가한다. timeout에서 쓰기 1회와 복구 안내를 검증했다. PRD 14.2의 일반적인 부분 성공 조건 전체가 증명된 것은 아니다.
- Macro preview: 전체 응답 재전송을 제거했다. 적용 전 티켓과 비교해 명시된 쓰기 필드의 변경값과 새 comment만 PUT에 포함한다. 조회 시각을 승인 payload에 묶고 `safe_update`/`updated_stamp`로 충돌을 방지한다. 전체 preview에서 읽기 전용·미변경 필드 제외 및 승인 후 timestamp 변경 시 outbound write 0회를 테스트했다. 모든 Macro action이 이 쓰기 필드 집합에 정확히 대응하는지는 별도 API action 대조가 남는다.
- 전체 pagination: Community 기본 목록과 content subscriptions 및 게시글 votes는 공통 cursor 수집기를 사용한다. 사용자 votes는 혼합 원본 1000건 scan cap과 opaque 재개 cursor를 유지하며 최초 cursor 반복을 거부한다. posts/topics/search와 사용자 votes 재개를 live 검증했고, content subscriptions live는 단일 페이지만 확인했다. 배지 세 목록은 complete-list 수집기와 local offset을 사용한다. 각 기능의 전체 상한·실환경 다중 페이지 성공을 일부 endpoint 증거로 일반화하지 않는다.
- Guide/KB streaming: article 페이지는 임시 JSON Lines에 기록하고 JSON/CSV를 행별로 원자적 cache writer에 전달한다. KB는 export_article_artifact의 locale별 ResourceLink manifest만 1시간 캐시한다. Guide JSON/CSV 및 ticket CSV 합성 100000건 파일의 크기/건수/낮은 Python 추적 메모리와 CSAT 양 backend JSON/CSV 10000건 회귀 테스트를 확인했다. 실제 대용량 API 응답/RSS/다중 locale KB 전체 수명 검증은 남아 있으며 소량 live 및 합성 부하 증거와 구분한다.
- CSAT 실제 접근: survey의 잘못된 .json 경로를 공식 suffixless 경로로 수정해 실제 목록 0건/정상 종료를 확인했다. Legacy는 현재 permission_denied다. 404를 계정의 survey 미지원으로 분류하지 않는다. 모든 고객 응답 목록과 export metadata에 untrusted_user_content 표시를 추가했다.
- 원격 검증: 로컬 테스트 통과는 GitHub CI 성공 증거가 아니다. 원격 CI 결과와 실제 Windows 검증이 필요하다.
- 쓰기 E2E: 운영 계정의 읽기 인증 성공은 disposable sandbox의 승인된 쓰기 검증을 대체하지 않는다.

## 이번에 검증한 경계

- Groups pagination: MCP limit/cursor 입력과 표준 items/has_more/next_cursor/truncated 응답을 연결했다. 기본 100건·호출 최대 1,000건·페이지 최대 100건을 적용한다. 다중 페이지 수집·빈 마지막 페이지·반복 커서 거부 테스트와 저장된 OAuth의 실제 두 페이지(각 1건) 읽기를 확인했다. 외부 next URL은 따라가지 않는다. 나머지 Operations 목록은 여전히 위 미완료 항목이다.
- Audit 파일 안전성: symlink와 다중 hardlink를 거부하며 열린 descriptor가 일반 파일인지 확인한 후에만 chmod/write한다. 지원 플랫폼에서 O_NOFOLLOW와 O_NONBLOCK으로 링크 경쟁·FIFO 대기를 제한한다. 링크 대상의 내용·기존 권한 보존을 테스트했다. Windows fchmod/ACL 보완을 대신하는 변경은 아니다.
- Windows cache identity: getuid가 없는 플랫폼은 SystemRoot/System32의 whoami.exe `/user /fo csv /nh`에서 SID를 읽고 경로 안전한 SID 형식만 허용한다. export/attachment 생성·정리 모두 같은 resolver를 사용하며 식별 실패 시 공용 사용자 경로로 fallback하지 않는다. 함수 부재와 명령 응답을 모사한 SID 분리·path traversal 거부 테스트를 추가했다. 실제 Windows 실행 및 ACL 보호는 별도 미검증이다.
- Windows SID 실패 경계: timeout, 명령 비정상 종료, 실행 파일 부재 각각에서 attachment/export 생성은 표준 upstream 오류, 두 cache cleanup은 안전한 무동작임을 테스트했다. 기존 다른 사용자 파일 보존과 새 공용 cache 미생성을 확인했다.
- KB 리소스: locale별 export의 `truncated`를 응답에 보존하며, 잘못된 articles/truncated 타입은 빈 정상 결과 대신 upstream 오류로 반환한다. 여러 locale 전체에 100,000건 상한을 적용하고 남은 한도를 다음 locale에 전달한다. 생략한 locale이 있으면 전체 `truncated=true`다. 99,999건 뒤 다음 locale에 1건만 요청하고 세 번째 locale을 호출하지 않는 테스트를 통과했다. KB의 전체 메모리 사용은 아직 별도 작업이다.
- Ticket artifact: format 지정 시 기존 Search Export page adapter와 projection을 재사용해 여러 페이지를 spool로 저장한다. 기본 100,000건 상한에서 `truncated=true`와 재개 cursor를 반환한다. 전체 과정의 반복 upstream cursor를 거부하고 페이지 크기·meta 타입을 검증한다. 다중 페이지 파일 내용, 100,000건 중단과 cursor 재개를 로컬 테스트했다. 실제 OAuth Search Export는 위 scope 충돌로 여전히 미검증이다.
- 누적 변경 재검증(2026-09-11): 별도 `UV_PROJECT_ENVIRONMENT`에서 Python 3.10.17, 3.11.16, 3.12.5 각각 `uv run --python VERSION --frozen pytest -q --tb=short` 299 passed. 새 임시 디렉터리로 sdist/wheel을 빌드하고 별도 Python 3.12 환경에 정확한 wheel을 설치한 뒤 저장소 밖 cwd에서 stdio 초기화와 도구 101개를 확인했다. 이는 macOS 로컬 증거이며 Windows, 원격 CI, 운영 쓰기 성공을 증명하지 않는다.
- CSAT artifact: [Legacy](https://developer.zendesk.com/api-reference/ticketing/ticket-management/satisfaction_ratings/)와 [Survey](https://developer.zendesk.com/api-reference/ticketing/ticket-management/csat_survey_responses/)의 cursor 응답으로 여러 페이지를 읽는다. 기존 backend별 filter resolver를 재사용하며 파일 spool과 streaming serializer로 저장한다. 두 backend의 이전 페이지 메모리 해제, 반복 커서 거부, 100,000건 상한과 `truncated`를 검증했다. 실제 계정 CSAT 권한 및 대량 live export는 검증하지 않았다.
- OAuth 콜백: 잘못된 state, Host, path, 중복 code/error, code와 error 혼합, 비ASCII state, 빈 code를 거부한 뒤 정상 콜백으로 완료한다.
- 수동 OAuth: 비ASCII state를 표준 구성 오류로 거부하고 기존 유효 state는 유지한다.
- 다운로드 인증 경계: tenant→CDN redirect 후 CDN 401에서 Authorization 미전달·Zendesk refresh 0회·추가 replay 없음, Guide 이미지 refresh 실패에서 reauthorization_required·비밀 메시지 제거·HTTP 1회를 검증했다. 전체 359 passed.
- 다운로드 OAuth 401: 인증된 host의 401에 한해 response를 닫고 refresh 1회 후 재요청한다. 전체 3회 요청 예산에 포함하며 두 번째 401은 추가 갱신하지 않는다. old→new Authorization 및 계속 401일 때 갱신 1회 테스트 포함 전체 357 passed. 외부 CDN에는 이 분기를 적용하지 않는다.
- 첨부 cache 공개 경합: os.link가 FileExistsError를 반환할 때 destination을 lstat 재검증하지 않던 경로를 수정했다. 공개 직전에 symlink가 삽입되는 실패를 재현하고 validation_error, 링크 대상 내용 보존, 임시 파일 정리를 확인했다. 전체 355 passed. 모든 부모 디렉터리 TOCTOU·플랫폼 ACL의 완전한 해결을 뜻하지 않는다.
- 첨부 streaming 실패 수명: 64KiB를 실제 cache에 쓴 뒤 timeout이 나는 transport를 재현했다. 재시도 대기 전에 response close와 임시/최종 파일 부재를 확인하고, 다음 성공 본문만 최종 파일에 남음을 검증했다. 별도 크기 초과는 1회 요청 후 validation_error이며 임시/최종 파일이 없다. 전체 354 passed. 실계정 파일은 아직 다운로드하지 않았다.
- 첨부 HTTP→cache streaming 연결: download_attachment의 store callback에 64KiB 제한 청크 iterator를 넘기고 TicketTools는 원자적 cache writer를 직접 연결한다. 전체 content bytes를 MCP 도구 경로에서 만들지 않는다. 기존 이미지/bytes 소비자는 별도 store 없이 유지한다. 실제 httpx 스트림→cache 파일 내용/크기 테스트 및 첨부 소속·만료·검사 회귀 포함 전체 352 passed. 실계정 파일 다운로드와 실패 재시도 중 원자적 파일 수명은 추가 검증 대상이다.
- 첨부 cache 청크 저장: 기존 bytes 입력을 유지하면서 Iterable[bytes]를 직접 임시 파일에 기록한다. 누적 20MiB 제한·청크 타입 검증·fsync 후 원자적 공개를 유지한다. 여러 청크의 파일 내용과 중간 OSError 시 최종 파일 미생성/임시 파일 정리를 테스트했다. 전체 351 passed. 현재 TicketTools 네트워크 경로는 아직 bytes 반환을 사용하므로 end-to-end streaming 완료가 아니다.
- Guide/CSAT MCP pagination 통합: 7개 도구·8개 경로(legacy/survey 포함)를 실제 CallToolRequest→dispatch→GuideTools로 실행했다. HTTP 경계만 대체해 limit/cursor, built_in/applicable, query, backend별 filter와 최종 structuredContent를 검증한다. 전체 350 passed. 운영 권한이나 네트워크 E2E를 대신하지 않는다.
- 다운로드 중간 본문 실패 검증: 실제 SyncByteStream이 일부 bytes를 yield한 뒤 ReadTimeout을 발생시키도록 했다. 첨부/이미지 모두 대기 전에 이전 stream을 닫고, 다음 시도의 본문만 반환하며 이전 조각·size가 섞이지 않음을 확인했다. 전체 349 passed. transport 경계 모사이며 실계정 파일 다운로드 증거는 아니다.
- 첨부/이미지 공통 다운로드: _download_file로 크기 제한·응답 읽기·429/5xx/transport 재시도·30초 sleep 예산을 공유한다. 첨부만 기존 최대 3회 redirect 응답 제한과 URL 검증을 적용하며 retry 횟수와 별도로 관리한다. tenant→CDN→503→CDN 재시도에서 Authorization이 CDN에 없음을 검증했다. 두 다운로드의 429/503/timeout 복구 포함 전체 347 passed. bounded bytes 응답이므로 최종 파일까지의 메모리 상수 streaming 요구사항을 완료한 것은 아니다.
- Guide image read retry: 429/5xx/timeout/HTTP transport 오류에서 최초+최대 2회 재시도하며 429 Retry-After+jitter 및 총 sleep 30초 예산을 적용했다. response context를 종료한 뒤 대기하고 크기/URL 오류는 재시도하지 않는다. 429·503·timeout 후 성공, 20초 반복 예산 중단, 60초 즉시 반환 테스트 포함 전체 343 passed. 첨부 다운로드의 redirect와 retry 통합은 남아 있다.
- Presigned upload 적용 상태: 인증/권한/429 거부(401·403·429)는 not_applied로, 서버 실패는 unknown으로 반환한다. 세 거부 상태의 기존 unknown 실패를 재현 후 수정했다. 각 상태별 PUT 1회·Authorization 미전달 테스트 포함 전체 338 passed. 외부 저장소 업로드에 자동 재시도를 추가하지 않았다.
- 다운로드 429 공유: Retry-After 기록을 _record_cooldown으로 공통화하고 tenant 첨부·검증된 Guide 이미지 응답에도 연결했다. 각각 60초 429 뒤 API 요청이 실제 전송되던 실패를 재현 후 차단했다. 전체 334 passed. CDN 응답은 tenant cooldown에 합치지 않는다. 다운로드 자체의 제한된 자동 재시도는 아직 남아 있다.
- 다운로드 cooldown 진입 차단: 공통 _cooldown_failure를 API 요청·tenant 첨부 다운로드(각 redirect 단계)·검증된 Help Center 이미지 다운로드에 적용했다. 기존 cooldown 중 추가 다운로드 2건이 전송되는 실패를 재현하고 outbound 0회로 수정했다. 전체 332 passed. 다운로드 자체가 받은 429를 공유 상태에 기록하는 경로와 재시도 통합은 아직 남아 있다. 외부 CDN은 tenant cooldown 대상으로 합치지 않았다.
- 공용 cooldown 동시 응답 검증: 두 실제 스레드를 Barrier로 같은 시점의 in-flight 요청에 진입시킨 뒤 60초 응답 처리 완료 후 10초 응답이 도착하도록 Event로 제어했다. 20초 시점의 새 호출은 outbound 0회, 60초 시점은 허용됨을 검증하여 짧은 응답의 deadline 덮어쓰기를 보호한다. 전체 331 passed. 실제 Zendesk 쓰기는 수행하지 않았으며 다운로드/업로드 적용은 여전히 별도다.
- 공용 cooldown 1차 구현: ZendeskClient.request의 tenant별 deadline을 프로세스 내 Lock으로 공유한다. 유효한 Retry-After(30초 초과 포함)를 기억하며 새 호출은 cooldown 동안 outbound 0회로 rate_limited/not_applied를 반환한다. 기존 읽기 재시도·30초 sleep 예산과 쓰기 무재시도는 유지한다. 서로 다른 client의 공유·독립 tenant·만료 후 허용 테스트 포함 전체 330 passed. 별도 다운로드/업로드 transport 경로의 적용 및 동시 in-flight 응답 경합 검증은 아직 남아 있어 PRD 공용 limiter 전체 완료로 보지 않는다.
- list_csat 일반 조회: 백엔드별 필터 검증·단일 페이지 요청을 _csat_page로 분리해 기존 export와 공유한다. 일반 조회에는 공통 cursor 수집기 및 MCP limit/cursor를 연결했다. legacy/survey 두 백엔드에서 후속 페이지 필터 유지 테스트 포함 전체 329 passed. export의 파일 streaming 경로는 유지한다. 실제 CSAT 접근 권한 검증은 별도 남아 있다.
- 기존 satisfaction ratings 목록: 공통 cursor 수집기와 MCP limit/cursor를 연결했다. 재개 테스트 포함 전체 328 passed. 실제 OAuth limit=1 요청은 permission_denied이므로 live 성공은 미검증이다. backend별 필터를 받는 list_csat의 일반 조회 표준화는 별도 남아 있다.
- 최신 wheel 검증: /tmp/zendesk-package-current-NDh0IG 에 새 sdist와 wheel을 빌드하고 독립 Python 3.12 venv에 정확한 wheel 경로로 설치했다. 저장소 밖 cwd에서 pagination 모듈이 설치 경로에서 import됨을 확인하고 stdio initialize, 도구 101개, 최근 변경 도구의 limit/cursor schema를 검증했다. mcp 1.30.0 설치 환경이다. macOS 로컬 패키지 검증이며 게시·원격 CI·Windows 실행은 수행하지 않았다.
- 누적 변경 Python 호환성 재검증: 각각 새 임시 UV_PROJECT_ENVIRONMENT를 사용해 Python 3.10.17·3.11.16·3.12.5에서 `uv run --python VERSION --frozen pytest -q --tb=short`를 실행했다. 세 환경 모두 327 passed. 기존 작업용 venv를 변경하지 않았다. macOS 로컬 실행 증거이며 Windows·Linux·원격 CI·새 wheel 설치 검증을 대신하지 않는다.
- Guide article search: 공식 offset 전용·최대 1,000건 계약에 맞춰 공통 수집기의 endpoint별 상한을 전달한다. query/brand_id/locale 필터와 MCP limit/cursor를 연결했다. 999 위치의 마지막 결과 및 1000 cursor 거부 테스트 포함 전체 327 passed. 결과 변동에 따른 offset 제약은 MCP 설명에 명시했다.
- Guide user segments: 일반/applicable 목록에 공통 cursor 수집기와 MCP limit/cursor를 연결했다. 공통 함수의 filters 인자를 통해 built_in=false도 모든 후속 페이지에 유지한다. 두 endpoint의 다중 페이지 필터 유지 테스트 포함 전체 326 passed.
- Offset 항목 검증: 사용자 검색과 Guide 권한 그룹에 쓰는 공통 수집기도 객체가 아닌 항목을 upstream_error로 거부한다. cursor/offset에 동일한 입력 경계 테스트를 적용해 offset의 실패를 확인 후 수정했다. 전체 325 passed.
- Guide permission groups: live read에서 page[size]=1은 무시되고 per_page=1/page=1은 1건과 next_page를 반환함을 확인했다. 사용자 검색의 offset 수집기를 pagination.py로 추출·재사용하고 MCP limit/cursor를 연결했다. 전체 324 passed. offset의 데이터 변동 제약은 도구 설명에 명시했다.
- 공통 cursor 응답 검증: 목록 내부의 null/string/number/list/bool을 upstream_error로 거부한다. 후속 페이지가 잘못된 경우 앞 페이지 결과를 정상 완료로 반환하지 않는 회귀 테스트를 추가했다. 실패 재현 후 수정했으며 전체 323 passed. Operations/Guide의 공통 수집기 사용 경로에 적용된다.
- Guide categories/sections: cursor 수집기를 pagination.py로 옮겨 Operations와 공유한다. 브랜드별 _get 경로를 유지하며 두 목록과 MCP에 limit/cursor를 연결했다. 재개·브랜드 endpoint 회귀 테스트 포함 전체 321 passed. Guide의 나머지 목록/검색은 별도 미완료다.
- Operations MCP pagination 통합 검증: 목록/검색 11개 도구를 실제 CallToolRequest → server dispatch → MetadataTools 경로로 호출한다. 외부 HTTP 경계만 대체하며 endpoint·query·page/limit/cursor 전달과 structuredContent의 재개 결과를 검증한다. 기존 단순 AnyTools 등록 테스트가 잡지 못하던 인자 누락 회귀를 보호한다. 전체 320 passed. 실제 네트워크·권한 검증을 대신하지 않는다.
- Custom statuses: 공식 API는 pagination 없이 전체 undeleted 상태를 반환한다. MCP는 이를 검증 후 기본 100건·최대 1,000건으로 잘라 표준 목록 응답과 local offset cursor를 제공한다. 재조회 중 상태 변경 시 위치 이동 가능성을 도구 설명에 명시했다. 재개·잘못된 cursor·잘못된 목록 테스트 포함 전체 319 passed, 실제 OAuth 1건 조회 성공. 이전 Custom statuses 미완료 메모 중 목록 표준화 항목을 대체한다.
- Ticket forms pagination: 공식 Ticket Forms 문서의 cursor 입력 지원에 맞춰 공통 목록 처리와 MCP limit/cursor를 연결했다. 기존 표 기반 재개 테스트에 forms를 추가해 실패 후 수정·통과를 확인했다. 전체 317 passed, 실제 OAuth limit=1 조회 1건 성공. Custom statuses의 목록 표준화는 남아 있다.
- User search pagination: offset 전용 API에 고정 per_page=100과 결과 위치 cursor를 적용했다. 호출 최대 1,000건, 쿼리 최대 10,000건을 유지하고 페이지 내부 재개·다음 페이지 수집을 지원한다. next_page URL은 요청하지 않고 같은 고정 endpoint의 page 숫자만 증가한다. 한도 도달 시 truncated=true와 next_cursor=null로 표시한다. 동일 query 재개 및 데이터 변동에 따른 offset 이동 제약을 MCP 설명에 명시했다. 전체 317 passed, 저장된 OAuth로 사용자 검색 limit=1 실제 성공. 전체 기능 완료 증거는 아니다.
- Group users pagination: [공식 Users 문서](https://developer.zendesk.com/api-reference/ticketing/users/users/#list-users-by-group)의 cursor 지원에 맞춰 공통 목록 처리와 MCP limit/cursor 입력을 연결했다. 잘못된 group_id 거부·재개 커서 테스트 포함 전체 315 passed. 저장된 OAuth로 실제 그룹의 사용자 1건 읽기 성공. 사용자 검색은 같은 문서상 offset 전용이므로 cursor 구현을 재사용하지 않았다.
- Brands/Ticket Fields pagination: 공식 문서의 cursor 지원을 확인하고 공통 목록 처리와 MCP limit/cursor 입력을 연결했다. 재개 커서 회귀 테스트 포함 전체 314 passed. 저장된 OAuth로 brands limit=1은 실제 1건 성공, ticket_fields limit=1은 permission_denied였다. 필드 조회의 live 성공은 미검증이며 권한을 자동 확대하지 않았다.
- Search Export 정렬은 [공식 Search 문서](https://developer.zendesk.com/api-reference/ticketing/ticket-management/search/#export-search-results)의 `created_at` 고정 순서를 따른다. 별도 정렬 파라미터를 추가하지 않는다.
- 공통 cursor 수집의 무한 빈 페이지 방지: 유효한 새 cursor가 있는 빈 중간 페이지에서는 추가 요청을 멈추고 기존 items 및 next_cursor와 truncated=true를 반환한다. terminal/null 경계와 반복 cursor 거부는 유지한다. 최초/중간 빈 페이지의 요청 중단과 다음 호출 재개를 테스트로 먼저 실패시킨 뒤 수정했다. 전체 361 passed, git diff --check 통과. 이 검증은 공통 수집기를 사용하지 않는 별도 export/Community 루프의 완료 증거는 아니다.
- Presigned upload 전송은 bytes 외에 청크 iterator를 지원한다. 실제 httpx 전송 경계에서 지연 소비·청크 순서·Authorization 미전달·503 재시도 없음을 검증했다. 실패 재현 후 수정, 전체 362 passed. Community 파일 로더는 아직 bytes를 반환하므로 사용자 이미지/badge의 end-to-end streaming은 미완료다. 다음 작업은 승인 SHA-256에 결합한 private 파일 snapshot과 청크 iterator 연결 및 모든 반환 경로의 정리 검증이다.
- Community 이미지/badge 업로드는 공통 승인/전송 경로를 사용하며, 로컬 파일을 TemporaryFile에 64 KiB 단위로 복사하면서 2 MB 한도와 SHA-256을 검증한다. 승인과 일치한 사본을 iterator로 전송하고 preview/apply 반환 시 닫는다. 전송 준비 중 원본 변경에도 사본 유지, apply 전 변경 시 outbound 0회, 초과 크기 및 승인 불일치 시 사본 정리를 검증했다. stream read OSError는 상세 경로 노출 없이 unknown 상태로 반환하고 재시도하지 않는다. 전체 364 passed, diff check 통과. 실제 업로드 E2E와 부모 디렉터리 교체 TOCTOU/Windows 파일 보호 검증은 여전히 남아 있다.
- Local image path 보강: 검사 후 부모 디렉터리를 외부 경로 symlink로 교체하면 root 밖 파일이 preview되는 실패를 재현했다. root anchor부터 각 경로 요소를 O_DIRECTORY/O_NOFOLLOW와 dir_fd로 열고, 최종 파일을 O_NOFOLLOW/O_NONBLOCK 및 fstat로 검증한다. ExitStack이 열린 descriptor를 정리한다. 실제 FIFO 거부·descriptor 닫힘·부모 symlink 교체 거부 및 보안 primitive 미지원 시 unsupported/not_applied를 검증했다. 전체 367 passed. Windows secure upload 구현은 없으며 fail-closed다. root 밖 데이터를 가리키는 hardlink 정책과 실제 OS별 E2E는 별도 감사 대상이다.
- Image upload 準備 응답 검증: 2xx에 upload 객체가 없거나 token이 빈 값이면 실제 binary 전송 없이 성공하는 경로를 재현·수정했다. 누락 객체/빈 객체/빈 token·id는 upstream_error와 unknown으로 반환하고 준비 요청 이후 PUT/최종 생성은 호출하지 않는다. 준비 POST의 실패는 그대로 전파한다. 전체 370 passed, diff check 통과. 실제 Zendesk mutation E2E는 수행하지 않았다.
- User Image 마지막 생성 응답: [공식 Create Image Path](https://developer.zendesk.com/api-reference/help_center/help-center-api/user_images/#create-image-path)의 user_image.path를 확인한다. 누락/빈 객체/빈 경로/외부 URL/파일명 없는 경로를 성공으로 반환하던 테스트를 먼저 실패시킨 뒤 upstream_error·unknown으로 수정했다. 3단계 요청을 반복하지 않으며 정상 응답은 보존한다. 전체 375 passed, diff check 통과. 실제 생성 성공은 로컬 응답 검증으로 대체되지 않는다.
- Community posts 목록: 공통 cursor 수집기로 기본 100/최대 1000건, page size 100 이하, 필터·scope 유지, 재개 cursor/truncated를 연결했다. 전체/토픽별/사용자별 101건 2-page 수집의 실패를 재현 후 수정하고 MCP schema/설명도 맞췄다. 전체 378 passed, diff check 통과. 다른 Community 목록과 offset 검색은 이 변경에 포함되지 않으며 후속 검토 대상이다.
- Community comments/topics: 게시글별·사용자별 댓글과 토픽 목록을 공통 cursor 수집기로 연결했다. 기본 100/최대 1000건, 페이지 100 이하, 중간 cap의 truncated/cursor와 다음 호출 재개를 101+1건 테스트로 검증했다. MCP schema/설명과 기존 한 페이지 테스트를 총 결과 수 계약으로 맞췄다. 전체 381 passed, diff check 통과. 검색·다른 Community 목록의 전환은 별도 남아 있다.
- Community search: [공식 Search Posts](https://developer.zendesk.com/api-reference/help_center/help-center-api/help_center_search/#search-posts)의 /api/v2/help_center/community_posts/search.json과 results를 사용한다. offset 공통 수집기에 endpoint page_size=25를 연결하고 query 유지, 페이지 내부 offset 재개, 최대 1000개 및 이후 truncated=true/next_cursor=null을 구현했다. MCP limit/cursor도 연결했다. 문서의 일반 pagination 문구에 100 articles가 있으나 해당 endpoint의 25 posts 제한을 따른다. 26개 후 재개하여 1000개 경계에서 41번째 페이지를 요청하지 않는 테스트 포함 전체 382 passed. 실제 OAuth 조회는 후속 검증 대상이다.
- Community live read: 저장된 OAuth로 posts/topics/search 각각 limit=1의 실제 조회가 ok=true, count=1, has_more=true, truncated=true였다. 각 도구를 새로 처음부터 조회한 후 반환 cursor로 재개하여 모두 ok=true/count=1이고 첫 ID와 다른 결과임을 확인했다. 검색은 공식 help_center/community_posts/search.json과 25건 offset 수집기가 실환경에서 동작함을 확인했다. 출력에는 본문·ID·cursor·개인정보 없이 상태/건수/중복 여부만 기록했다. 댓글 및 나머지 Community 목록, 전체 한도 live 검증, 쓰기 E2E는 이 증거에 포함되지 않는다.
- Community content tags 검색: 기존 공통 cursor 수집기를 재사용해 prefix를 매 페이지 유지하고 기본 100/최대 1000건을 수집한다. MCP schema/설명과 cap의 truncated 응답을 연결했다. 101건 2-page 필터 유지 및 cursor 재개 테스트 포함 전체 383 passed, diff check 통과. 실제 태그 조회와 다른 목록의 전체 검증은 별도 남아 있다.
- User subscriptions 목록: followers/followings 양쪽에 공통 cursor 수집기를 적용했다. 기본 100/최대 1000건, direction 유지, 101건 수집 후 마지막 1건 재개를 검증하고 MCP schema를 맞췄다. 전체 385 passed, diff check 통과. 실제 OAuth의 해당 목록 권한과 badge 계열 목록 pagination은 미검증이다.
- Badge list live 경로 결함: 저장 OAuth로 badge_categories/badges/badge_assignments의 .json 경로는 모두 not_found였으나 공식 suffix 없는 경로는 세 개 모두 ok=true였다. 세 목록 경로를 수정하고 회귀 테스트를 추가했다. 전체 386 passed, diff check 통과. 따라서 초기 not_found는 요금제 미지원 증거가 아니다. 단건/생성/수정/삭제에도 .json 문자열이 남아 있어 공식 경로 정합성 후속 수정이 필요하다. 실제 mutation은 수행하지 않았다. 공식 배지 문서는 전체 목록을 기술하며 별도 pagination parameter를 명시하지 않는다.
- Gather 나머지 경로: 공식 Badge/Badge Category/Badge Assignment 문서에 맞춰 단건·POST·PUT·DELETE의 .json 접미사를 제거했다. 기존 승인/gate와 payload 검증은 유지하고 경로 회귀 테스트 4개의 실패 후 수정·통과를 확인했다. 전체 386 passed, diff check 통과. 저장 OAuth로 실제 목록에서 얻은 category와 badge 각각의 단건 GET도 성공했다(본문/ID 출력 없음). POST/PUT/DELETE live 검증은 수행하지 않았다.
- Badge icon update 통합 검증: 기존 구현에 null 제거 지원이 있어 중복 구현 없이 실제 CallToolRequest → dispatch → CommunityTools → ApprovalStore → 전송 경계 테스트를 추가했다. icon_upload_id 생략/null/새 ID의 차이, preview outbound 0회, 승인 apply PUT 1회와 suffix 없는 경로를 검증했다. 테스트 서버에는 community,badges capability를 명시해야 한다. 전체 387 passed, diff check 통과. 외부 HTTP만 대체했으며 실제 badge 수정은 하지 않았다.
- 2026-09-12 compatibility 재검증: 새 격리 venv /tmp/zendesk-verify-310-KlqNJK/venv의 Python 3.10.17과 /tmp/zendesk-verify-311-exrWFT/venv의 3.11.16에서 각각 387 passed. 기존 개발 환경도 387 passed, git diff --check 통과. uv build로 최신 sdist 및 그 sdist에서 wheel 생성 성공: /tmp/zendesk-build-verified-0qVkxX/zendesk_mcp_server-1.0.0-py3-none-any.whl. 새 wheel 설치 후 stdio 검증은 아직 수행하지 않았다. macOS 내 Python 버전 검증이며 Linux/Windows CI 증거가 아니다.
- 2026-09-12 wheel 설치/stdio: /tmp/zendesk-build-verified-0qVkxX/venv(Python 3.12.5)에 직전 wheel을 설치(mcp 1.30.0)하고 /tmp에서 실행했다. pagination 모듈이 설치 venv에서 import됨을 resolved path로 확인했다(macOS /tmp→/private/tmp 차이는 검증 스크립트에서 정규화). 실제 bin/zendesk로 MCP initialize/list_tools가 성공해 Zendesk/101 tools를 반환했다. 최신 Community 6개 schema의 maximum=1000 및 limit=1001 입력 거부를 확인했다. Zendesk 읽기/쓰기 요청을 검증하는 호출은 하지 않았다. 배포/원격 CI 완료 증거가 아니다.
- Badge 목록 제한: 공통 collect_array로 badge/categories/assignments의 객체 배열을 검증하고 기본 100/최대 1000건 및 local offset cursor를 반환한다. 원격 요청에는 기존 필터만 보내며 재개 시 전체 배열을 다시 조회한다. 데이터 변경에 따른 offset 이동 제약을 MCP 설명에 명시했다. 1002개 배열에서 100개 이후 재개·필터 유지·한도 초과 거부 테스트 및 schema/dispatch 연결 포함 전체 390 passed, diff check 통과. 새 envelope의 실환경/stdio 검증은 별도 남아 있다.
- Badge 목록 검증: 실제 OAuth에서 categories/badges 각각 limit=1 조회 및 반환 offset으로 재개한 1건이 성공하고 첫 ID와 달랐다. assignments는 ok=true/0건으로 빈 목록만 확인했다. 실제 CallToolRequest → CommunityTools 통합 테스트는 세 목록의 limit=1/cursor=1, 브랜드·사용자·배지·카테고리 필터가 고정 endpoint에만 전달되고 두 번째 항목/다음 cursor가 반환됨을 검증한다. 전체 391 passed, diff check 통과. live assignments 재개와 쓰기 E2E는 미검증이다.
- Content subscriptions: 공식 Content Subscriptions의 cursor 지원에 맞춰 post/topic 목록을 공통 수집기에 연결했다. 기존 content ID 검증과 endpoint를 유지하며 기본 100/최대 1000건 및 MCP limit/cursor를 추가했다. 101건 이후 마지막 1건 재개 테스트 포함 전체 393 passed, diff check 통과. 실환경 subscriptions 응답 및 MCP dispatch 통합은 별도 검증 대상이다.
- Content subscription live/MCP: 실제 OAuth로 조회한 post/topic 각각의 subscriptions(limit=1)이 ok=true/count=1/has_more=false였다. 실제 CallToolRequest → dispatch → CommunityTools 테스트로 post/topic 고정 경로, limit=1/cursor=before, 표준 재개 응답을 확인했다. 전체 394 passed, diff check 통과. live 결과는 각각 한 페이지로 종료되어 다중 페이지 실환경 재개를 입증하지 않는다. 실제 구독 생성/수정/삭제는 수행하지 않았다.
- User votes 경계: 새 cursor를 가진 빈 중간 페이지는 무한 수집 대신 truncated=true/scanned_count=0와 opaque 재개 cursor를 반환한다. 후속 호출 재개를 테스트했다. 객체 아닌 vote, 요청 page size 100 초과, bool이 아닌/누락 has_more를 upstream_error로 거부한다. 실패 재현 후 수정, 전체 399 passed 및 diff check 통과. 사용자 투표의 총 limit=100 제한과 게시글별 투표의 한 페이지 처리, 최초 resume cursor 반복 검사는 아직 별도 남아 있다.
- Votes 총 한도: post/user 모두 limit 최대 1000과 MCP schema를 연결하고 게시글별은 공통 cursor 수집기로 전환했다. 두 scope의 150건 수집 테스트 포함. 사용자별은 혼합 원본 scan cap 1000을 유지하며, 짧은 페이지 이후 1050건까지 읽던 결함을 남은 예산 page size로 수정했다(50+9*100+50 검증). 전체 402 passed, diff check 통과. 최초 resume cursor 반복 및 user 결과 제한 시 truncated 의미 정합성은 후속 감사 대상이다.
- User votes 재개/표시: 초기 resume boundary를 seen에 포함하고, 페이지 내 결과 제한으로 반환하기 전에 upstream next cursor를 검증한다. 반복 boundary를 가진 1건/2건 페이지의 실패를 각각 재현 후 수정했다. 페이지 내부 또는 다음 페이지에 남은 결과가 있어 limit으로 중단하면 truncated=true를 반환하며, terminal 페이지 내부 재개 후 정상 종료도 유지한다. 새 회귀 4개 포함 `uv run pytest -q --tb=short` 406 passed, `git diff --check` 통과. 실제 투표 API 재개 및 나머지 요구사항의 live 검증을 입증하는 결과는 아니다.
- Votes live 경로/재개: 실제 OAuth에서 기존 help_center/posts/{id}/votes.json은 permission_denied였으나 [공식 List Post Votes](https://developer.zendesk.com/api-reference/help_center/help-center-api/votes/#list-post-votes)의 community/posts/{id}/votes.json은 성공했다. 회귀 테스트 실패 후 경로를 수정했고 수정된 도구도 실제 ok=true/0건을 반환했다. 사용자별 limit=1 이후 opaque cursor 재개는 ok=true/1건이며 첫 결과와 다른 ID임을 확인했다. 출력에는 ID/본문/cursor를 포함하지 않았다. 전체 407 passed, diff check 통과. 게시글 투표의 비어 있지 않은 목록·다중 페이지 live 및 쓰기 E2E는 이 검증에 포함되지 않는다.
- User subscription 사전/사후 조회 경계: upsert와 read-back의 공통 _find_user_subscription에서 누락/비정상 has_more 및 객체 아닌 항목을 upstream_error로 거부한다. 빈 중간 페이지는 무한 조회나 구독 없음 판단 대신 안전하게 실패한다. 짧은 첫 페이지 이후 scan이 1050건까지 늘어나던 문제는 남은 1000건 예산을 page size에 적용해 수정했다. 공개 upsert 진입점의 회귀 4개 실패 후 수정, 전체 411 passed 및 diff check 통과. 실제 구독 변경은 하지 않았으며 live 쓰기/사후 검증 완료 증거는 아니다.
- User subscription read-back 실패 검증: 기존 outcome_unknown 구현을 재사용하고 공개 upsert → 실제 로컬 승인 저장소 → POST 전송 경계 → read-back의 테스트를 추가했다. 변경 후 malformed page/구독 없음/설정 불일치 세 경우 모두 outcome_unknown, operation_state=unknown, retryable=false이며 POST는 정확히 한 번이다. preview는 쓰기 0회다. 전체 414 passed, diff check 통과. 외부 HTTP 경계만 대체한 로컬 검증이며 실제 Zendesk 구독은 변경하지 않았다.
- Image upload HTTP framing: 승인 snapshot의 알려진 file_size를 공통 업로드 경로에서 Content-Length로 전달한다. 원격 headers의 불일치 길이 또는 Transfer-Encoding은 PUT 전에 upstream_error/unknown으로 거부한다. 두 이미지 흐름의 64 KiB stream/길이 검증과 httpx 실제 Request의 Content-Length 및 Transfer-Encoding 부재를 확인했다. 인증 미전달/503 쓰기 재시도 없음도 유지한다. 전체 417 passed, diff check 통과. [공식 User Images](https://developer.zendesk.com/api-reference/help_center/help-center-api/user_images/)의 presigned PUT 흐름을 확인했으나 실제 업로드 서버 수락까지 검증한 것은 아니다.
- 최신 package 재검증: /tmp/zendesk-package-current-vPLy5H에 sdist와 그 sdist 기반 wheel을 새로 만들고 별도 Python 3.12.5 venv에 설치했다. /tmp에서 설치된 bin/zendesk의 실제 stdio initialize/list_tools로 Zendesk/101개 도구와 votes maximum=1000을 확인했다. pagination 모듈이 해당 설치 환경에서 import됨도 확인했다. 전체 개발 테스트 417 passed, diff check 통과. 원격 CI/Python 3.10·3.11의 최신 변경 검증이나 live 쓰기 완료를 의미하지 않는다.
- 완료 조건 재대조: PRD 14.2는 여전히 전체 미입증이다. server.py의 opt-in knowledge-base resource는 locale별 export_articles 결과를 articles 배열로 누적하고 최종 JSON 문자열을 1시간 캐시한다. 대규모 KB의 메모리·ResourceLink 계약은 별도 확인/수정 대상이며 현재 package handshake만으로 충족을 주장하지 않는다.
- Article export 공통 수집기: KB inline export와 파일 artifact가 함께 쓰는 _export_article_pages에서 객체 아닌 article 및 누락/비정상 has_more를 거부한다. has_more=true인 빈 페이지는 무한 조회 대신 upstream_error로 중단한다. 세 회귀 실패 후 수정했고 전체 420 passed, diff check 통과. KB의 전체 배열/문자열 캐시는 아직 그대로이므로 대용량 메모리 및 파일 출력 전환 완료로 해석하지 않는다.
- KB 파일 전환: opt-in zendesk://knowledge-base는 기존 export_article_artifact를 locale별 재사용하고 본문 대신 item_count/truncated와 직렬화된 ResourceLink를 담은 JSON manifest를 반환한다. MCP read_resource는 Text/Blob contents만 반환하므로 링크는 JSON manifest 안의 resource 필드다. 총 100000건 한도와 1시간 manifest 캐시를 유지하고 파일은 기존 24시간 관리 캐시를 사용한다. 실제 GuideTools→파일 생성→MCP resource 응답 테스트에서 링크의 파일 본문과 inline 본문 부재를 검증했다. 기존 inline articles 배열 소비자는 resource.uri 파일을 읽도록 이전해야 한다. 최신 전체 테스트 결과는 다음 검증에서 기록한다.
- KB 전환 검증: 전체 421 passed, diff check 통과. docs/migration.md에 inline articles에서 locale resource.uri로 소비자를 이전하는 절차와 TTL/총 한도를 기록했다. 실환경 대용량 KB 및 최신 wheel 검증은 별도 남아 있다.
- Export 파일 metadata: 공통 _cache_ticket_export가 원자 publish 전에 stream.tell()로 바이트 크기를 얻어 size와 mime_type을 반환한다. 재독출 없이 모든 티켓/Guide/CSAT/KB export metadata로 전달된다. 두 형식의 청크 크기 테스트 실패 후 수정, KB 실제 파일 크기와 manifest metadata 일치 검증 포함 전체 422 passed. 파일 링크 자체의 size 속성 등 전체 출력 계약 감사는 별도 남아 있다.
- KB TTL/버전 검증: 벽시계가 뒤로 조정되면 1시간 캐시가 연장되는 실패를 재현하고 프로세스 내 TTL 계산을 time.monotonic으로 변경했다. 3599초에는 재사용, 3600초에는 벽시계 역행에도 재생성한다. 현재 작업 트리 소스를 import하는 Python 3.10.17(/tmp/zendesk-verify-310-KlqNJK/venv), 3.11.16(/tmp/zendesk-verify-311-exrWFT/venv), 개발 Python 3.12에서 각각 전체 422 passed. macOS 로컬 검증이며 원격 CI/다른 OS 또는 최신 wheel 증거는 아니다.
- 첫 export 실환경 결함: OAuth locale 조회/API 페이지는 성공했지만 기본 ~/.cache/zendesk-mcp-server 부모가 없어 export cache could not be written이었다. 새 중첩 경로 회귀 실패 후 공통 저장기에 application parent 생성을 추가했다(mode 0700, 기존 root/user symlink 검사 유지). 전체 423 passed, diff check 통과. 수정 후 두 locale의 실제 제한 1건 export가 각각 성공했고 결과는 0건과 1건이었다. 파일 크기/건수와 ResourceLink를 확인했다. 고객 본문/ID/경로는 검증 출력에 포함하지 않았다. 대용량 export 성공을 뜻하지 않는다.
- 첫 attachment 캐시: export와 동일하게 상위 application 폴더가 없는 새 설치에서는 첨부 저장도 실패했다. 공개 download_attachment 진입점에서 소속 확인/가짜 HTTP 다운로드/실제 디스크 저장을 거치는 테스트로 재현 후 parent mkdir을 추가했다. 새 application 폴더 0700 및 저장된 바이트 일치, 기존 symlink/스트림 안전성 테스트 포함 전체 424 passed, diff check 통과. 실제 고객 첨부 다운로드는 수행하지 않았다.
- Export 기존 디렉터리 권한: mkdir(mode=0700)는 기존 0755 폴더를 제한하지 않아 사용자 전용 캐시 계약에 부족했다. attachment 캐시의 기존 방식과 같이 root/user 폴더의 symlink/type 검사 후 chmod 0700을 적용한다. 기존 두 폴더 0755를 사용한 회귀 실패 후 수정, 폴더 0700/파일 0600 및 전체 425 passed, diff check 통과. POSIX mode 검증이며 Windows ACL 또는 부모 경로 동시 교체 방어 완료 증거는 아니다.
- Export 충돌 보존: 최종 이름이 이미 있으면 os.replace가 기존 export를 덮어쓰는 동작을 고정 이름 테스트로 재현했다. attachment에서 사용하는 os.link 기반 원자 no-clobber publish를 재사용하고 기존 finally의 임시 파일 정리를 유지했다. 충돌은 upstream_error이며 기존 파일 바이트를 보존한다. 전체 426 passed, diff check 통과. 이름 난수 충돌 확률과 무관하게 기존 결과를 덮어쓰지 않는 계약을 검증했다.
- 원격 CI 실사: gh run list/view로 현재 저장소의 최신 성공 run 33842922670을 확인했다. headSha=325ff0aed5d52852461c988fff1e368596b80bb0, 2026-09-04의 main 실행이며 build 단일 job만 포함한다. 현재 HEAD cad756f 및 미커밋 변경, 현재 .github/workflows/ci.yml의 Linux test/wheel/handshake와 macOS/Windows package-smoke matrix를 입증하지 않는다. 진행 중인 현재 변경의 CI handle도 확인되지 않았다. 읽기만 수행했으며 commit/push/PR/원격 실행은 하지 않았다. 따라서 원격 CI 조건은 명확히 미검증으로 유지한다.
- Export ResourceLink 크기: 공통 _export_content에서 유효한 비음수 정수 size를 MCP ResourceLink.size에 전달한다. 기존 metadata 없는 결과는 size 생략을 유지한다. 티켓 링크의 실패 재현 후 수정했고 실제 KB 파일의 크기와 JSON manifest 안 resource.size 일치도 확인했다. 전체 426 passed, diff check 통과. 별도의 파일 재조회나 전체 메모리 로딩은 추가하지 않았다.
- CSAT export 페이지 검증: legacy/survey 공통 export 경로에서 객체 아닌 항목과 has_more=true인 빈 페이지를 upstream_error로 거부한다. 정상 첫 페이지 뒤 malformed/진전 없는 두 번째 페이지를 두 backend에서 재현했고, 실패 시 부분 JSON 파일이 발행되지 않는 회귀 4개를 추가했다. 전체 430 passed, diff check 통과. 실제 CSAT 권한 및 데이터 조회 성공을 대신하는 검증은 아니다.
- Ticket export 페이지 검증: projection 전에 객체 아닌 results를 거부하여 누락/잘못된 성공을 방지한다. 파일 export는 has_more=true인 빈 페이지에서 upstream_error로 중단하며 정상 첫 페이지가 있어도 부분 파일을 발행하지 않는다. 단일 페이지 API의 재개 계약은 유지했다. 두 회귀 실패 후 수정, 전체 432 passed와 diff check 통과. OAuth search/export scope 충돌과 live 대규모 export 검증은 남아 있다.
- Ticket search 총 limit: 기존 한 페이지/100건 clamp에서 공통 collect_offset로 전환해 기본 100·최대 1000건 총 제한과 query별 1000건 상한을 적용한다. 기존 page/per_page 의미와 첫 page 파라미터 생략을 유지하고 페이지 내부 재개에는 offset next_cursor를 반환한다. page!=1과 cursor 동시 입력은 거부한다. projection은 수집 후 적용하며 외부 next URL은 따라가지 않는다. 150건+나머지 50건 재개 실패 재현 후 수정, MCP schema/dispatch 연결 포함 전체 433 passed, diff check 통과. 실제 MCP dispatch 통합 및 OAuth 검색 성공은 별도 검증 대상이다.
- Ticket search MCP 통합: 실제 CallToolRequest→dispatch→TicketTools→offset 수집기로 limit=150/cursor=950/projection fields=id를 검증했다. 10번째 고정 API page에서 50개만 반환하고 1000 경계의 has_more/truncated=true와 재개 없음, 외부 next URL 미호출을 확인했다. 총 한도/legacy page/페이지 중간 cursor의 소비자 이전 안내를 docs/migration.md에 추가했다. 전체 434 passed, diff check 통과. live OAuth 검색은 여전히 권한 정책 충돌로 미검증이다.
- Ticket list 총 limit: 기존 단일 페이지 처리를 공통 collect_cursor로 대체하고 MCP 기본 100/최대 1000건 및 truncated 의미를 연결했다. 150건의 100+50 수집 실패 후 수정, 전체 435 passed/diff check 통과. 실제 OAuth로 limit=1 조회와 반환 cursor 재개가 각각 성공해 서로 다른 1건을 반환했다(본문/ID/cursor 출력 없음). 실제 1000건 전체 수집과 MCP dispatch 통합은 이 증거에 포함되지 않는다.
- Conversation pagination: get_conversation을 공통 cursor 수집기로 연결해 data.comments를 유지하고 has_more/next_cursor/truncated 및 기본 100/최대 1000 입력을 추가했다. 첨부 목록(따라서 다운로드 소속 확인)과 이슈 문맥은 _complete_conversation으로 모두 수집하며 반복/빈 중간 결과 또는 100000 댓글 한도에서는 불완전한 성공 대신 실패한다. 댓글 attachments 누락 KeyError도 get 기본값으로 수정했다. 두 번째 페이지의 댓글/첨부/문맥과 limit=1 재개를 검증하고 migration을 기록했다. 전체 436 passed, diff check 통과. 상단 대화 항목 중 첫 페이지만 조회하던 문제는 해결했으나 채널/side 정규화와 실제 MCP/live 검증은 아직 남아 있다.
- Conversation MCP/live: 실제 CallToolRequest의 limit=1/cursor를 고정 comments endpoint로 전달하고 원본 public 및 untrusted 표시를 보존하는 통합 테스트를 추가했다. 후속 page permission_denied는 첨부 목록/다운로드/이슈 문맥으로 전파되며 다운로드를 시작하지 않음을 검증했다. 전체 438 passed, diff check 통과. 실제 OAuth에서도 티켓 한 개의 댓글 1건과 커서 재개의 다른 댓글 1건이 성공했다. 출력에는 본문/ID/cursor를 포함하지 않았다. 채널 통합/side 정규화는 아직 미완료다.
- Conversation channel: [Ticket Comments](https://developer.zendesk.com/api-reference/ticketing/tickets/ticket_comments/)의 metadata.via와 [Via reference](https://developer.zendesk.com/documentation/ticketing/reference-guides/via-object-reference/)의 문자열/숫자 channel 형식을 대조했다. get_conversation은 원본 channel이 없으면 top-level via 또는 metadata.via의 channel을 평탄화하며 원본 author_id/public/side/metadata는 유지한다. 알려진 7개 채널·미지 문자열·숫자의 테스트 실패 후 수정, 전체 439 passed 및 diff check 통과. 원본 side가 없는 경우의 작성자 역할 연결과 chat/messaging transcript 내부 정규화는 아직 완료하지 않았다.
- Conversation author/side: 공식 Ticket Comments의 include=users를 각 페이지 요청에 추가하고 일치하는 작성자의 현재 role로 author_role 및 누락된 side(customer/agent/unknown)를 채운다. public 여부로 추측하지 않으며 원본 side/author_role은 보존한다. 삭제 사용자 문자열·잘못된 ID는 역할 추론에서 제외하고 잘못된 users collection은 upstream_error로 처리한다. 누락 요청의 실패 재현 후 수정, 페이지 간 역할 연결/원본 보존/알 수 없는 작성자와 MCP 출력 포함 전체 442 passed 및 diff check 통과. 실제 OAuth 댓글 1건에서도 author_role/known side/untrusted 표시를 확인했다(고객 본문/ID 출력 없음). 현재 역할을 과거 작성 시점 역할로 보장하지 않으며 chat/messaging transcript 내부 정규화는 여전히 미완료다.
- Time tracking pagination: 단일 audits 요청을 collect_cursor로 연결하고 include_boundary_indicators=true 및 MCP limit/cursor를 추가했다. limit은 시간 기록 수가 아닌 스캔한 audit 수이며 빈 entries에도 has_more/next_cursor를 유지한다. 후속 페이지 재개 실패 후 수정, 실제 MCP dispatch를 포함한 전체 444 passed, diff check 통과. 현재 연결의 실제 limit=1 조회는 성공했고 시간 항목은 0개였다. 실제 후속 페이지/archived ticket 검증은 남아 있다. 공식 Ticket Audits 문서는 global OAuth read 필요 및 archived pagination 미지원을 명시한다. 권한은 확대하지 않았으며 현재 성공을 모든 OAuth 권한 조합의 보장으로 해석하지 않는다.
- 최신 버전/패키지 재검증: 현재 변경으로 Python 3.10 및 3.11 전체 테스트 각각 444 passed. 새 /tmp/zendesk-package-verify-ygTLF7에서 sdist→wheel 빌드 후 별도 Python 3.12.5 환경에 정확한 wheel을 설치했다. 저장소 밖 실행 및 site-packages import를 확인하고 설치된 zendesk 명령으로 실제 MCP initialize/list_tools를 수행해 Zendesk/101 tools와 conversation/time-tracking의 limit 최대 1000 및 cursor schema를 확인했다. 이전 417-test 시점 wheel이 아닌 현재 패키지의 증거다. 이 결과는 macOS 로컬이며 Windows/Linux 실행, 원격 CI, 배포 또는 실제 mutation 성공을 입증하지 않는다. Windows에서 없는 os.fchmod의 audit.record 경로는 여전히 검토할 구현 항목이다.
- Audit unsupported-platform 응답 보존: os.fchmod를 제거한 실제 MCP CallToolRequest 테스트에서 정상 결과가 AttributeError MCP 오류로 바뀌는 것을 재현했다. AuditLog.record는 descriptor 권한 설정이 없으면 파일/디렉터리 생성 전에 중단하고 식별자/본문 없는 static unsupported 경고를 남긴다. 전체 445 passed 및 diff check 통과, 원래 structuredContent와 파일 미생성을 확인했다. Windows의 감사 저장 지원을 구현한 것은 아니며 ACL 기반 저장/실제 Windows 실행은 미완료다. token/config 권한 경계에는 변경이 없다.
- Macro effective payload gate: action 목록만으로는 실제 전송 comment.author_id/created_at과 status=closed의 위험을 놓쳤다. 최종 preview ticket도 검사하여 impersonation/destructive risk를 승격했다. 누락 실패 후 수정, 실제 ApprovalStore로 승인된 payload라도 해당 gate가 꺼져 있으면 write_disabled/outbound PUT 0회임을 검증했다. 전체 447 passed 및 diff check 통과. 모든 공식 macro action의 변환이나 실제 쓰기 E2E 완료 증거는 아니다.
- Macro recipient gate: 공식 actions-reference의 cc action을 PUBLIC 위험으로 연결했다. 최종 diff의 collaborator_ids/email_cc_ids/follower_ids/recipient 변경도 수신 대상 변경으로 PUBLIC 승격한다(제거 포함 보수적 처리). 누락 테스트 실패 후 수정했고 실제 ApprovalStore 승인 후에도 public gate가 꺼져 있으면 write_disabled/전송 0회를 확인했다. 전체 448 passed 및 diff check 통과. 알림의 실제 발생 여부나 모든 macro action 실행 지원을 입증한 것은 아니다.
- Ticket list 정렬: PRD 7.2의 정렬 입력 누락을 확인했다. live cursor 요청에서 id/-id/updated_at/-updated_at 각각 2건 및 해당 필드 순서가 확인됐고 created_at/-created_at은 실패했다. 확인된 네 값만 MCP와 list_tickets에 노출하며 모든 페이지에 sort를 유지한다. 생략 시 기존 기본값을 유지한다. 누락 입력 테스트 실패 후 구현, 2페이지 정렬 전달/미지원 값 거부 및 전체 449 passed, diff check 통과. 실제 cursor 재개의 정렬 연속성 및 변경 중인 데이터의 안정성 전체를 입증한 것은 아니다.
- Ticket sort MCP/live 재개: 기존 실제 MCP dispatch 테스트에 -updated_at을 넣어 limit/cursor/sort가 HTTP 경계까지 보존됨을 검증했다. live -id와 -updated_at 각각 limit=1 후 반환 cursor로 재개했고 서로 다른 티켓 1건 및 두 페이지 사이 내림차순을 확인했다. 본문/ID/cursor는 출력하지 않았다. 전체 449 passed 및 diff check 통과. 장시간 변경 중인 데이터나 모든 정렬 조합의 안정성을 보장하는 증거는 아니다.
- Git-Zen 커밋/마크업: PRD 7.2의 commit 링크 중 GitHub commit 경로가 정규식에서 빠져 있어 추가했다. GitLab issue/MR/commit 링크 뒤의 마크다운 괄호·HTML 문자가 URL에 포함되는 오류도 재현해 경로별 숫자 ID/hex commit 구분으로 수정했다. 설정 필드만 읽는 기존 경계와 중복 제거를 유지하며 두 실패 회귀 후 전체 451 passed 및 diff check 통과. 링크 대상에 네트워크 요청은 수행하지 않았고 self-hosted Git 호스트 지원을 의미하지 않는다.
- Issue context attribution: 기존 Markdown은 댓글 body만 붙여 author/channel/public/side를 소실했다. 정규화된 대화의 id/author_id/author_role/created_at/channel/public/side만 명시적 metadata로 넣고 응답에 untrusted_user_content=true 및 Markdown 경고를 추가했다. 실패 회귀 후 전체 452 passed/diff check 통과. html_body 별도 제외와 전체 페이지 소비는 유지한다. 이는 고객 본문 안 비밀값을 제거하는 sanitizer가 아니므로 PRD의 비밀값 제외 계약 전체는 여전히 추가 검증/보완이 필요하다.
- Issue context labeled credential redaction: 제목/설명/댓글을 합친 Markdown에 Bearer/Basic 인증값 및 명시적 token/api_key/client_secret/password 할당값 제거를 추가했다. JSON 인용값·공백 포함 인용 비밀번호·URL query 값·환경변수 표기를 실제 공개 메서드 테스트로 실패 재현 후 검증했고 일반 진단 문구와 다음 query 필드는 유지한다. 전체 453 passed 및 diff check 통과. 원본 Zendesk 내용은 변경하지 않는다. 이름 없는 임의 비밀값이나 모든 인코딩을 탐지하는 DLP 기능은 아니므로 비밀값 제외의 절대 보장을 주장하지 않는다.
- Context escaped credential: 인용된 비밀번호 내부의 escaped quote에서 기존 정규식이 종료돼 secret suffix가 남는 실패를 재현했다. 인용 문자열에서 escape pair를 함께 소비하도록 수정해 JSON double quote와 single-quoted assignment 모두 값 전체를 가리고 뒤의 일반 필드는 유지한다. 전체 454 passed 및 diff check 통과. 임의 인코딩/무명 비밀 탐지의 한계는 유지한다.
- Issue context prerequisite failure: 티켓 단건 조회가 실패해도 전체 대화 수집을 시작하던 순서를 수정했다. not_found/permission_denied/rate_limited 각각에서 원래 오류를 보존하고 comments 요청 0회임을 실패 재현 후 검증했다. 전체 455 passed 및 diff check 통과. 오류 이후 불필요한 네트워크/페이지 수집을 방지하며 성공 경로의 전체 대화 처리는 유지한다.
- Guide JSON streaming 용량 검증: 합성 1KB 본문으로 실제 export_article_artifact 파일 경로를 실행했다. 1,000건=1,047,781 bytes/peak Python 156,466 bytes/0.04s, 100,000건=105,177,781 bytes/peak 233,602 bytes/3.48s였다. 두 경우 item_count/truncated=false/디스크 크기와 metadata 일치를 확인했다. 임시 합성 파일은 TemporaryDirectory 종료로 제거했다. 10,000건 파일이 10MB를 넘더라도 tracemalloc peak 4MB 미만인 회귀 테스트를 추가했고 전체 456 passed/diff check 통과. Python 추적 메모리만 측정했으며 전체 RSS·실제 HTTP 응답·CSV/CSAT/ticket export 전체의 메모리 증거로 일반화하지 않는다.
- Guide/Ticket CSV 용량 검증: 두 공개 export 메서드를 합성 100,000건으로 실행했다. 각 파일 103,677,789 bytes, Guide peak Python 234,398 bytes/5.05s, Ticket peak 389,512 bytes/5.16s. CSV를 행별로 재읽어 100,000개 ID 순서와 item_count/truncated=false/크기 일치를 확인했다. 합성 임시 파일은 종료 시 정리했다. 기존 10,000건 메모리 회귀 테스트를 JSON/CSV 모두에 적용해 전체 457 passed/diff check 통과. 실제 HTTP/RSS/CSAT backend 전체 검증과는 구별한다.
- CSAT streaming 메모리 회귀: legacy/survey × JSON/CSV 네 조합의 실제 export_csat→spool→serializer→managed cache 경로를 각각 합성 10,000건으로 검증했다. 각 파일 10MB 초과, item_count=10,000/truncated=false/실제 크기 일치 및 tracemalloc peak 4MB 미만을 확인했다. 기존 두 페이지 내용/수명 및 cap 테스트와 함께 전체 461 passed, diff check 통과. HTTP는 합성 client이므로 실제 계정 CSAT 접근·대량 네트워크·전체 RSS 완료 증거는 아니다.
- CSAT live 경로 수정: legacy limit=1은 permission_denied, survey의 기존 .json 경로는 not_found였다. 공식 /api/v2/guide/survey_responses 경로로 동일 GET을 수행하면 성공(0건/has_more=false)했다. 공통 _csat_page의 suffix를 제거해 list/export 모두 수정하고 기존 HTTP 경계 기대값을 맞췄다. 실패 회귀 후 전체 462 passed/diff check 통과. 수정된 공개 list_csat도 실제 0건/has_more=false/truncated=false 성공. 따라서 기존 survey 404는 기능 미지원 증거가 아니라 잘못된 경로였다. 실제 응답이 있는 다중 페이지 및 legacy 권한은 여전히 미검증/제약 상태다.
- CSAT trust 표시: legacy ratings 편의 메서드를 list_csat 공통 경로로 연결하고 성공 목록 envelope 및 export data에 untrusted_user_content=true를 추가했다. 원본 응답 항목과 파일 직렬화는 유지한다. 고객 텍스트가 지시가 아닌 데이터라는 표시이며 sanitizer는 아니다. 누락 실패 후 수정, 기존 MCP 실제 dispatch 기대값에 표시를 포함하여 전체 463 passed/diff check 통과.
- 누적 변경 버전 재검증/도구 설명: 현재 Python 3.10과 3.11에서 각각 전체 463 passed. MCP ticket export의 오래된 single-page 설명을 실제 max 100000건 다중 페이지 ResourceLink 파일 생성과 page-size limit 의미로 수정했고 migration에 구분을 추가했다. 설명 수정 후 server 49 passed 및 diff check 통과. 새 Windows/원격 CI 또는 최신 wheel 설치 증거는 아니다.
- Macro fields alias: 공식 Show Ticket After Changes 예제는 fields 객체를 사용하며, 실제 read-only preview에서는 fields/custom_fields가 각각 24개인 동일 배열이었다(내용/ID 출력 없음). custom_fields가 없을 때 fields 객체 또는 배열을 검증해 custom_fields로 정규화하여 기존 diff/approval 경로에 포함한다. 누락 실패 후 수정, 두 wire shape의 preview/쓰기 0회 및 전체 464 passed/diff check 통과. 실제 쓰기는 하지 않았고 requester locale/custom user field 등 모든 action의 실행 지원은 별도 남는다.
- Macro fields 승인 적용 검증: fields 객체/배열 모두 실제 ApprovalStore 승인 뒤 PUT 경계에 custom_fields로 정확히 전달되고 safe_update/updated_stamp를 유지하는지 검증했다. null/문자열/boolean ID/value 누락/non-object 항목은 upstream_error이며 승인 파일 생성과 쓰기 모두 0회다. 이 검증은 외부 HTTP만 대체한 로컬 테스트이며 실제 Zendesk 변경은 수행하지 않았다.
- Macro canonical custom_fields 검증: fields alias만 검증하고 custom_fields 직접 응답은 null/비정상 항목도 승인하던 경로를 재현했다. alias 변환 이후 공통 검증으로 옮겨 두 형식에 동일한 배열/positive integer ID/value 존재 검사를 적용했다. 다섯 malformed case에서 승인 파일/쓰기 0회, 전체 466 passed/diff check 통과.
- CSV formula-like text 방어: 공통 _csv_value와 header 직렬화에서 수식 접두/선행 공백·제어문자/전각 접두 문자열에 apostrophe를 붙인다. 숫자 음수와 JSON 원문은 유지한다. 실패 회귀 후 전체 467 passed/diff check 통과. CSV reader로 셀 경계/변환을 확인했지만 실제 Excel·LibreOffice 실행/재저장 안전성을 입증하지는 않는다. OWASP가 경고하는 앱별 차이와 원문 필요 시 JSON 사용을 migration에 기록했다.
- Macro action 범위 정정/공유 gate: 공식 actions-reference를 섹션별로 대조했다. locale_id/requester.custom_fields 및 notification action들은 Additional actions for ticket triggers and automations에 있으므로 이를 매크로 필수 실행 누락으로 간주했던 후속 기록은 정정한다. 기존 방어적 위험 판정은 유지한다. 실제 현재 macro 목록 80개를 읽어 종료를 확인했고 body/value 없이 action 유형 수만 집계했다. 매크로 shared action인 share_ticket/follower와 최종 sharing_agreement_ids는 PUBLIC 위험으로 승격하도록 실패 재현 후 보완했다. 전체 467 passed/diff check 통과. 실제 공유·팔로워 추가는 수행하지 않았다. 모든 macro의 적용 결과를 80건 목록 읽기만으로 증명한 것은 아니다.
- 최신 wheel 검증(467-test 변경 포함): 새 /tmp/zendesk-release-check-qRMk4G에 sdist→wheel을 빌드하고 독립 Python 3.12.5 venv에 정확한 wheel을 설치했다. 저장소 밖 cwd/site-packages import를 확인하고 실제 설치 zendesk 명령의 stdio initialize/list_tools에서 Zendesk/101 tools와 정렬 schema를 검증했다. 설치 코드의 CSV 접두 처리·macro share_ticket PUBLIC 분류·CSAT suffixless 경로 및 untrusted 표시도 확인했다. Zendesk 외부 호출은 없었으며 release 게시·원격 CI·Windows 실행 증거가 아니다.
- Ticket count 응답 검증: Python bool이 int로 취급되어 true/false count와 음수를 정상 반환하던 경로를 실패 재현했다. 비음수 exact int만 허용하도록 수정해 bool/음수/문자열/실수/null은 upstream_error, 0과 양수는 유지한다. 전체 468 passed 및 diff check 통과.
- Content Tags 경로 대조: 공식 https://developer.zendesk.com/api-reference/help_center/help-center-api/content_tags/ 의 suffixless 경로로 검색/count/상세/생성/수정/삭제 6개 호출을 통일했다. 기존 경로 기대값을 공식 계약으로 교정해 5개 테스트 실패를 확인한 뒤 수정했으며 전체 468 passed. 저장된 OAuth의 읽기 전용 실호출에서 count=6, 검색 limit=1의 1개 반환을 확인했다. 생성/수정/삭제는 로컬 승인 및 HTTP stub으로만 검증했으며 실제 Zendesk 변경은 하지 않았다.
- Content Tags count 응답 검증: 누락된 value/비객체 data/불리언/음수/문자열/실수를 성공으로 전달하지 않도록 exact nonnegative int 검증을 추가했다. 정상 value와 refreshed_at, upstream 오류는 그대로 보존한다. 빈 객체의 잘못된 성공을 실패 테스트로 재현했으며 전체 469 passed. 실제 외부 변경은 수행하지 않았다.
- Community post content tag ID wire type: 실제 게시글 100건의 content_tag_ids 원소 28개가 str임을 값 노출 없이 확인했다. 공식 Posts 예제의 integer만 허용하던 update validator/MCP schema를 string 또는 positive integer로 확장하고 원래 타입을 보존한다. 기존 path-safe tag ID validator와 public 승인 경로를 재사용했다. 문자열 preview 거부를 실패 재현 후 수정, 잘못된 ID 거부/승인 후 PUT payload 보존 테스트 및 전체 470 passed, MCP mixed-ID schema 검증 통과. 운영 쓰기는 수행하지 않았다. 같은 점검의 content-tag search limit=100은 실패했으므로 pagination/page-size 원인 확인이 후속 작업으로 남는다.
- Content Tags page size 실호출 수정: 같은 prefix/경로에서 page[size]=1/10/30은 성공, 31/50/100은 HTTP 400임을 확인했다. 공통 cursor 수집기의 내부 page_size 인자를 재사용 가능한 기본 100으로 추가하고 content tags만 30을 전달한다. 사용자 전체 limit 1..1000은 축소하지 않는다. 101개를 30/30/30/11로 수집하는 실패 테스트 후 수정, 전체 470 passed. 수정 후 실제 기본 검색은 6건/has_more=false로 성공했고 limit=1 cursor 재개는 서로 다른 다음 항목을 반환했다. 실제 쓰기는 없었다.
- PRD 범위/라이선스 패키지 점검: PRD 8장의 post-tag 연결은 update 도구를 사용하므로 별도 create 옵션은 추가하지 않았다. 기존 wheel에는 LICENSE가 포함돼 있으나 License-Expression은 없었으며 pyproject에 Apache-2.0 및 LICENSE 포함을 명시했다. 새 /tmp/zendesk-license-check-983vod에서 sdist→wheel 빌드 성공, wheel metadata의 License-Expression/License-File 및 양 배포물의 LICENSE 바이트 일치를 확인했다. frozen 전체 470 passed. 이는 자체 LICENSE 패키징 증거이며 michaelrice 원본 코드/테스트의 복사 이력과 NOTICE 필요성 감사까지 입증하지 않는다. 공개 배포는 수행하지 않았다.
- Opaque ID 경로 검증: 태그/배지/배지 카테고리/할당에서 공유하는 _valid_tag_id가 dot segment, percent-encoded 경로 문자, 역슬래시와 내부 제어문자를 허용하던 문제를 수정했다. 7개 조회/삭제 소비자에서 10개 위험 입력을 거부하고 HTTP 호출 및 approval 파일 생성이 0회인 회귀 테스트를 실패 재현 후 통과시켰다. 기존 정상 문자열/정수 post tag payload 보존 테스트 포함 frozen 전체 471 passed. 운영 API에는 위험 ID를 전송하지 않았다.
- 누적 변경 호환성/설치 재검증(471-test 상태): 기존 격리 Python 3.10.17/3.11.16 환경이 현재 worktree src를 import함을 확인한 뒤 각각 전체 471 passed. 새 /tmp/zendesk-current-wheel-0Ltdgk에서 sdist→wheel을 빌드하고 독립 Python 3.12.5 환경에 정확한 wheel을 설치했다. 저장소 밖 cwd에서 site-packages import, Apache-2.0 metadata, 설치된 content-tag page size 30 및 dot-ID 거부를 확인했고 실제 zendesk stdio initialize/list_tools에서 Zendesk/101 tools 및 문자열 post-tag schema를 검증했다. 외부 Zendesk HTTP는 사용하지 않았고 원격 CI/Windows/쓰기 E2E/공개 배포 완료를 뜻하지 않는다.
- Upstream 출처 감사 1차: michaelrice/zendesk-mcp의 현재 main을 별도 /tmp/zendesk-upstream-audit-HcqBVC/upstream에 read-only 비교용으로 clone하고 SHA 8313e117094d005dcf1fc48ffb6f9197bc60a712를 고정했다. 해당 NOTICE에는 Copyright 2026 Michael Rice 및 개발자 attribution이 있고 LICENSE는 Apache-2.0이다. upstream src/tests Python 55개와 현재 src/tests를 빈 줄 제외·줄 양끝 공백 제거 후 연속 12줄 블록으로 비교해 동일 블록 0개를 확인했다. 이는 짧은 발췌, 이름 변경/재작성, 과거 upstream 버전의 복사 여부를 증명하지 않는다. 현재 git 이력의 auth/ticket 도입 커밋을 확인했지만 전체 패치별 출처는 미확인이다. 따라서 NOTICE가 불필요하다고 단정하거나 실제 소프트웨어 포함을 근거 없이 선언하는 NOTICE를 만들지 않았고, PRD 13.3 출처 release gate는 열린 상태다. 원본: https://github.com/michaelrice/zendesk-mcp/blob/8313e117094d005dcf1fc48ffb6f9197bc60a712/NOTICE
- Git-Zen 계약 점검: PRD는 설정된 필드의 issue/MR/commit 링크 추출까지만 요구하므로 외부 tracker 조회·생성은 추가하지 않았다. malformed ticket/custom_fields, 중복 ID, value 누락/비문자열을 빈 링크 성공으로 반환하던 문제를 실패 재현 후 upstream_error로 수정했다. 설정 ID가 티켓에 없으면 not_configured, 실제 null/빈 문자열/링크 없는 문자열은 정상 links=[]로 유지한다. frozen 전체 478 passed. 외부 tracker 및 운영 쓰기는 호출하지 않았다.
- Badge cascade preview 누락 수정: PRD 7.5의 배지 삭제 연쇄 할당 건수/복구 불가 표시가 없음을 확인했다. 공식 Badge Assignments의 badge_id 필터로 전체 배열을 읽어 건수와 정렬된 assignment ID hash를 로컬 승인 payload에 묶고 preview에 표시한다. apply 전 재조회에서 같은 건수라도 ID가 바뀌면 승인 불일치로 DELETE 0회다. malformed/중복/다른 배지/permission 오류는 승인 생성 없이 실패하며 gate 차단은 재조회 전 적용된다. 전체 480 passed. 원자적 read-delete API가 아니므로 최종 조회 이후 동시 변경 가능성은 migration에 명시했다. 운영 삭제는 하지 않았다. 공식 근거: https://developer.zendesk.com/api-reference/help_center/help-center-api/badges/ 및 https://developer.zendesk.com/api-reference/help_center/help-center-api/badge_assignments/
- Badge cascade MCP 통합 검증: 실제 Settings/build_community_tools/ApprovalStore 경로와 MCP CallToolRequest로 1,001개의 합성 할당을 미리보기했다. 반환 count=1001/irreversible/outbound_write=false, 계정별 로컬 승인 payload와 화면의 hash/count/badge ID 일치를 확인했다. 일반 목록 limit=1000으로 잘라 집계하지 않으며 read 1회/외부 write 0회다. MCP 도구 설명에도 최종 재조회와 동시 변경 한계를 명시했다. 전체 frozen 481 passed. 실환경 배지 삭제는 수행하지 않았다.
- Badge icon 변경 계약 재검증: 공식 API의 생략=유지/업로드 ID=교체/null=제거 동작은 기존 validator/schema/dispatch에 이미 구현돼 있어 로직을 추가하지 않았다. 실제 MCP CallToolRequest와 계정별 ApprovalStore를 통과시켜 세 가지 입력이 승인 payload 및 모의 PUT에 각각 그대로 보존됨을 확인했다. 도구 설명에 세 경우를 명시했고 frozen 전체 482 passed. 실제 아이콘 변경은 없었다.
- 완료 범위 재대조: PRD 7.2/13.2 원문과 현재 코드/테스트를 대조해 명시되지 않은 historical-role/transcript 재해석 및 허용 대안이 있는 Windows ACL 구현을 필수 누락으로 확대하지 않도록 위 목록을 정정했다. 대화 관련 회귀 8 passed. 이는 실환경 전채널/Windows 실행을 대신하는 증거가 아니며 OAuth search broad-read 정책 충돌, 원격 CI, 승인 sandbox 및 출처 검증의 미완료는 유지한다.
- 통합 필드 설정 오류 검증: Git-Zen의 int 변환은 기존 ValueError→ConfigurationError 처리가 있어 5,000자리 입력에도 status probe가 표준 validation_error/not_applied를 반환함을 확인했다. 추가 로직 없이 Git-Zen 및 Time Tracking 잘못된/부분 설정에 대해 원본 값이 응답에 포함되지 않는 회귀 테스트를 추가했다. frozen 전체 483 passed. 이 검증은 실제 인증이나 필드 접근 성공을 뜻하지 않는다.
- 플랫폼 실행 경로 재확인: 현재 host는 Darwin이며 PATH에서 docker/podman/limactl/colima 실행 파일을 찾지 못했다. 따라서 로컬 Linux 컨테이너 검증은 현재 제공된 실행 환경으로 진행할 수 없다. GitHub API의 최신 CI는 33842922670, completed/success, head 325ff0aed5d52852461c988fff1e368596b80bb0이며 현재 HEAD cad756f1a7aaaa1807effb87ec11de80f89e46db 및 25개 dirty path를 포함하지 않는다. 실행 중인 해당 작업 CI handle은 없다. 원격 CI/Windows 검증 완료로 주장하지 않으며 VM 설치나 push/CI 실행을 임의로 수행하지 않았다.
- OAuth 읽기 재확인: 저장된 OAuth로 tickets 목록 page[size]=1은 성공, search(type:ticket) 및 search/export는 permission_denied였다. 응답의 required_scopes는 error.details 안의 값이며 최상위 error에서 읽은 null을 scope 부재 증거로 사용하지 않는다. 현재 공식 grant_type_tokens 문서의 available scopes에는 account_settings:read가 ticket/user fields 조회용으로 명시돼 있으나 현재 operations scope 매핑에는 없다. 후속 작업은 capability별 실제 endpoint의 세부 scope를 재대조해 이 누락을 보완하는 것이다. 전역 read 금지 정책은 변경하지 않았고 재인증/추가 scope grant도 수행하지 않았다. 공식 근거: https://developer.zendesk.com/api-reference/ticketing/oauth/grant_type_tokens/#scope
- Operations scope closure 수정: MetadataTools의 실제 사용자/그룹/조직/브랜드/티켓 필드·폼/뷰/매크로·trigger 요청과 공식 grant scope 목록을 대조해 operations-only 구성에 users/groups/organizations/brands/tickets/account_settings:read가 빠졌음을 실패 테스트로 확인했다. 기존 view/macro/trigger scope에 세부 읽기 권한을 추가했고 전역 read/write는 요청하지 않는다. 저장 grant에 account_settings:read가 없으면 oauth_relogin_required이며 token 파일 bytes를 변경하지 않는 회귀도 확인했다. frozen 전체 485 passed. 실제 사용자는 Operations가 활성화된 기존 OAuth를 다시 로그인해야 하며 자동 grant/실환경 성공 검증은 하지 않았다. migration에 안내했다.
- 조건부 scope closure/최소화: git_zen 및 time_tracking 단독 구성에서 tickets:read가 누락돼 빈 scope가 만들어졌고 Guide-only standard 구성에는 tickets:write가 불필요하게 추가됐다. 5개 조합의 실패 테스트 후 두 조건부 capability의 ticket read scope를 추가하고 ticket/hc write scope를 실제 활성 도구군으로 제한했다. 기존 사용자 grant는 변경하지 않았고 global read/write는 추가하지 않았다. frozen 전체 490 passed. 기타 capability의 보조 endpoint 권한 대조와 실제 재로그인 후 접근 검증은 별도 남는다.
- Guide scope closure: _for_brand의 /api/v2/brands/{id}.json 조회에 필요한 brands:read가 Guide-only 구성에서 빠졌음을 인증 URL scope 실패 테스트로 확인했다. Guide에 brands:read를 추가하고 public-write 구성에도 불필요한 tickets:write 없이 브랜드/HC scope만 남는 것을 검증했다. frozen 전체 490 passed, 별도 brand 회귀 통과. 기존 토큰을 변경하거나 자동 재로그인하지 않았으며 실환경 brand 접근 성공의 증거는 아니다.
- Survey 페이지 크기 수정: 실제 조회에서 50까지 성공하고 51 이상은 HTTP 400이었던 기본 요청 크기 오류를 공통 `_csat_page`에서 50으로 제한했다. legacy의 100건 요청은 유지한다. auto/survey 양쪽에서 101건을 조회(50/50/1)하고 내보내기(50/50/50)하는 회귀 테스트로 전체 결과가 잘리지 않음을 확인했다. 전체 492 passed. 저장된 OAuth로 guide capability의 기본 Survey 조회가 성공(0건, has_more=false)했으며 실제 다중 페이지 데이터나 쓰기를 검증한 것은 아니다. 전체 기능 완료 판정은 유지하지 않는다.
- Survey 수정 포함 설치 재검증: 격리 Python 3.10/3.11 환경에서 현재 소스 전체 테스트 각각 492 passed. 새 `/tmp/zendesk-survey-package-6Dp7G9`에서 sdist→wheel을 빌드하고 독립 Python 3.12.5 환경에 정확한 wheel 경로로 설치했다. 저장소 밖 `/tmp`에서 설치 환경 내부 import와 Survey 기본 요청 50건 제한을 확인했으며 실제 설치된 `zendesk` 명령으로 stdio initialize/list_tools가 Zendesk/101개를 반환했다. macOS 로컬 증거이며 Linux/Windows, 원격 CI, 실계정 쓰기 또는 공개 배포 완료를 뜻하지 않는다.
- Time Tracking malformed snapshot 차단: 동일 configured field ID의 value 누락 항목이 중복돼도 기존 필터가 이를 무시해 갱신하던 경로를 재현했다. 이제 ID 기준으로 유일성을 확인한 뒤 value 존재를 검사하며 비객체 custom_fields 항목도 거부한다. 공통 조회 검증이 log_time에도 적용돼 두 필드 각각의 malformed 중복 및 null/string 항목에서 upstream_error와 outbound write 0회를 검증했다. 전체 493 passed. 실제 Zendesk 쓰기는 수행하지 않았다.
- User Subscription 검증 타입 보강: Python의 1==True/0==False 비교 때문에 숫자 include_comments를 기존 설정 일치 또는 쓰기 검증 성공으로 인정하던 경로를 재현·수정했다. bool identity로 두 판단 경로를 모두 제한한다. 숫자 기존 값은 idempotent 성공으로 처리하지 않고 승인 preview를 유지하며, 쓰기 후 숫자 또는 누락 값은 outcome_unknown과 자동 재시도 없음으로 처리한다. 공식 User Subscriptions 문서의 응답 예제에는 include_comments가 생략돼 있어 실제 응답에서 값이 없는 경우에도 성공을 추정하지 않는다. 전체 497 passed; 실제 구독 변경은 수행하지 않았다.
- User Subscription live 읽기 구조 확인: community capability로 me/followers 8건·has_more=false, me/followings 0건·has_more=false를 확인했다. followers 8건 모두 include_comments가 없었고 고객 본문/ID는 출력하지 않았다. 이는 nonempty followings 또는 POST 후 응답에서 설정 검증 가능함을 입증하지 않는다. migration에 outcome_unknown 시 자동 재시도 금지와 수동 확인 경로를 명시했다. 기본 capability 구성은 여전히 validation_error이므로 이전 OAuth 인증 성공을 현재 전체 capability 정상 구성으로 일반화하지 않는다. 실제 쓰기/권한 확장은 하지 않았다.
- OAuth scope expansion 오류 분류 수정: 현재 저장된 기본 grant의 실패가 OAuth scope expansion requires login again임을 확인했다. oauth_relogin_required를 기존 reauthorization_required 계약으로 매핑하고 capability gate도 공통 구성 오류 변환기를 사용하도록 했다. status·4개 도구 factory·capability gate에서 재인증 필요/not_applied/retryable=false를 검증했으며 전체 498 passed. scope/credential 변경 또는 자동 로그인은 수행하지 않았다.
- 재인증 CLI/MCP 경로 검증: 임시 계정의 guide-only grant를 실제 save_connection으로 저장한 뒤 기본 capability의 `zendesk check --probe`와 MCP CallToolRequest를 실행했다. 두 경로의 structured error가 동일한 reauthorization_required/not_applied임을 확인했고, CLI exit 1, HTTP send 0회, 저장 파일 bytes 불변을 검증했다. Settings.load를 대체하지 않은 저장 구성 통합 테스트이며 전체 499 passed. 실사용자 재로그인을 실행한 증거는 아니다.
- Custom Object 경로 검증: projection key와 티켓 lookup value가 경로에 그대로 들어가던 경로를 재현했다. 공통 projection 입력에서 key를 URL unreserved 문자로 제한하고 dot segment를 거부하며, search/export 공통 record 확장에서도 양의 정수 또는 같은 안전 문자열 ID만 허용한다. 경로 이동·query·fragment·percent encoding·backslash·container/bool 값은 record GET 전에 upstream_error로 중단한다. 공식 Custom Objects/Custom Object Records 문서를 대조했으며 전체 500 passed, 외부 레코드 요청이나 데이터 변경은 수행하지 않았다. 다중 lookup field/metadata pagination 검토는 아직 별도 항목이다.
- Custom Object 다중 lookup 누락 수정: 동일 객체를 가리키는 4개 field(서로 다른 record 2개, 중복 1개, null 1개)에서 마지막 null만 남아 결과가 비던 문제를 재현했다. search/export 공통 확장이 모든 field를 수집하고 객체별 중복 ID 조회를 제거한다. CSV도 마지막 record로 덮어쓰지 않고 다중 record를 열별 JSON 배열로 보존하며 누락 field는 null로 정렬한다. 기존 단일 record scalar CSV는 유지한다. 두 호출 경로의 records·조회 횟수·CSV 배열 내용 회귀 포함 전체 501 passed. 실제 Custom Objects 계정 접근 및 metadata 전체 pagination 검증은 아직 남는다.
- Custom Object metadata pagination 수정: ticket_fields 첫 응답만 사용하던 경로를 공통 collect_cursor로 연결했다. 전체 field 목록을 1,000건 chunk/100,000건 안전 상한으로 수집한 뒤 lookup을 해석하며 반복 cursor·정체·상한은 누락된 key로 처리하지 않고 upstream_error로 중단한다. 두 번째 페이지의 lookup field를 이용한 record 확장 회귀와 기존 search/export 다중 lookup 테스트 포함 전체 502 passed. 기존 stub 3개에도 공식 cursor meta를 추가했다. 실계정 ticket_fields 권한 검증은 재로그인 이후 남아 있다.
- Custom Objects OAuth metadata scope 보완: Operations 없이 projection을 사용하는 경우 ticket_fields 접근 권한이 요청 합집합에서 빠져 있었다. 공식 grant_type_tokens scope 표의 account_settings:read(ticket/user fields 포함)에 맞춰 custom_objects capability에 추가하고 exact scope 테스트로 broad read/write가 추가되지 않음을 확인했다. migration에 재로그인 필요를 명시했고 전체 503 passed. 기존 저장 grant는 변경하지 않았고 실계정 재인증/endpoint 성공을 입증하지 않는다.
- Custom Object lookup response 검증: custom_fields=null에서 TypeError, 비정상/중복 field에서 무시 또는 마지막 값 선택하던 경로를 수정했다. search/export 공통 확장은 list/object/양의 정수 ID/value 존재/ID 유일성을 확인한 뒤에만 record GET을 수행한다. null·dict·null 항목·list ID·bool ID·value 누락·중복 ID의 두 호출 경로에서 upstream_error와 record GET 0회를 검증했다. 전체 504 passed; 실제 데이터 변경 없음.
- Custom Object 실제 nested CSV 구조 보완: 공식 record의 custom_object_fields가 기존 CSV에서 JSON cell 하나로만 출력돼 object_key.field_key 열이 없던 문제를 수정했다. nested field를 개별 열로 추가하되 record 속성과 충돌하면 custom_object_fields namespace를 유지한다. 기존 raw JSON cell/JSON export는 보존하며 단일 문자열 formula 방어·다중 record 배열·누락 값 null 정렬을 검증했다. 전체 505 passed. 공식 문서: https://developer.zendesk.com/api-reference/custom-data/custom-objects/custom_object_records/
- Custom Object MCP 전체 로컬 경로: 실제 Settings/factory를 유지하고 HTTP client만 stub으로 바꿔 CallToolRequest 검색과 CSV export를 검증했다. ULID record ID 보존, projection 선택 필드+중첩 record, managed ResourceLink, cache 내부 경로, 실제 CSV asset.serial 열을 확인했다. write 경로 호출 시 실패하도록 했으며 전체 506 passed. 실계정 OAuth Search 권한과는 독립된 로컬 통합 증거다.
- TicketQuery 사용자 resolver pagination: 첫 users/search 페이지만 보고 사용자 1명을 확정하던 오류를 재현했다. 공식 offset-only/100개 page/10,000개 query 한도에 맞춰 기존 collect_offset을 재사용하며 전체 읽기가 상한에서 중단되면 ID 직접 지정을 요구하는 upstream_error로 처리한다. search/count/export 세 경로에서 다음 페이지의 동일 이름 후보가 있으면 candidate_ids [8,9]와 validation_error이며 티켓 쿼리 0회를 확인했다. upstream next_page의 외부 URL은 따라가지 않는다. 전체 507 passed. 브랜드/그룹/폼 resolver의 별도 cursor 경계 점검은 남아 있다.
- 이름 resolver 공통 cursor 검증: brands/groups/forms의 meta 누락 또는 잘못된 has_more를 완료로 취급해 대상 ID를 확정하던 문제를 재현했다. 기존 collect_cursor를 사용하는 collect_complete_cursor로 세 경로와 Custom Object field 수집을 공유하고 중복 while 구현을 제거했다. 100,000개 상한·정체/반복 cursor 중단을 유지한다. 세 entity × meta 누락/빈객체/숫자/문자열에서 티켓 검색 전에 upstream_error를 검증했으며 전체 508 passed. 실제 endpoint의 정상 메타데이터와 권한 검증을 대체하지 않는다.
- Organization resolver 계약 대조: 공식 organizations/search는 case-insensitive exact name 배열 응답이며 별도 pagination 계약이 없어 cursor를 추가하지 않았다. 대신 malformed 조직 목록이 후보 없음 또는 임의 정상 후보 선택으로 처리되던 문제를 고쳤다. list/object/정수 ID/name 타입 오류는 upstream_error와 티켓 쿼리 0회, 정상 무메타데이터 응답은 기존 exact-name 해석을 유지한다. 전체 509 passed. 공식 문서: https://developer.zendesk.com/api-reference/ticketing/organizations/organizations/#search-organizations
- 공통 complete cursor 경계/버전 재검증: 실제 collect_cursor를 통과하는 합성 100,000건은 완전 성공, 100,001건은 100,000건/1,000요청에서 upstream_error로 중단하고 부분 items를 노출하지 않음을 확인했다. 빈 nonterminal page는 HTTP 1회 후 실패한다. 누적 변경을 현재 macOS Python 3.12와 격리 3.10/3.11 환경에서 전체 실행해 각각 512 passed. 원격 Linux/Windows CI 및 현재 코드의 새 wheel 설치 검증은 별도다.
- 512-test 상태 새 설치 검증: `/tmp/zendesk-lookup-package-C7pcI3`에서 sdist→wheel을 만들고 별도 Python 3.12.5 venv에 정확한 wheel을 설치했다. 저장소 밖 /tmp에서 site-packages import, Custom Object nested 다중 CSV 배열, complete cursor helper, 설치 zendesk --help의 login/check 및 실제 stdio Zendesk/101개 도구를 확인했다. macOS 로컬 검증이며 Linux/Windows·remote CI·실계정 writes·release 게시 완료 증거가 아니다.
- 초기 검증 명령: `uv run pytest -q --tb=short` — 당시 255 passed. 최신 결과는 위 후속 항목에 기록하며, 테스트 통과는 미완료 항목을 완료로 바꾸지 않는다.
